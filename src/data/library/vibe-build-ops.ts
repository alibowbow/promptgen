import type { LibraryPrompt } from '../types';

export const prompts: LibraryPrompt[] = [
  {
    id: "vibe-ops-01",
    title: "앱과 DB를 Docker로 컨테이너화하기",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["Docker", "컨테이너화", "docker-compose", "배포준비"],
    prompt: `너는 컨테이너 배포 경험이 많은 시니어 DevOps 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

컨테이너화할 앱: [앱 설명 — 예: REST API 서버]
기술 스택: [언어/런타임/프레임워크, 예: Node.js 20]
데이터베이스: [DB 종류와 버전, 예: PostgreSQL 16]
런타임에 필요한 환경변수: [DB 접속정보, 시크릿 키 등 이름만 나열]

먼저 아래를 계획으로 정리하고, 내가 승인하면 파일을 만들어줘.
1. 만들 파일 목록(Dockerfile, .dockerignore, docker-compose.yml, .env.example)과 각 역할
2. 이미지 빌드 전략과 예상되는 실패 지점(빌드가 느려지는 원인, 캐시 무효화, 이미지 비대화 등)

그다음 아래 원칙을 반드시 지켜서 구현해줘.
- 멀티스테이지 빌드: 빌드 단계와 실행 단계를 분리해 최종 이미지에는 런타임에 필요한 것만 남긴다
- 가능한 한 작은 베이스 이미지(slim/alpine 등)를 쓰고 버전을 고정한다
- 시크릿(비밀번호/토큰)은 절대 이미지에 굽지 않는다. 빌드 인자나 COPY로 넣지 말고 런타임 환경변수로 주입한다
- 컨테이너는 root가 아닌 전용 사용자로 실행한다
- .dockerignore로 node_modules, .git, .env, 테스트 산출물 등을 빌드 컨텍스트에서 제외한다
- HEALTHCHECK로 앱의 실제 준비 상태(예: /health 응답)를 확인한다
- docker-compose에 앱과 DB를 정의하고, DB 데이터는 named volume으로 영속화하며, 앱은 DB가 healthy가 된 뒤 시작하도록 depends_on 조건을 건다
- 앱은 SIGTERM을 받으면 진행 중 요청을 마치고 커넥션을 닫는 graceful shutdown을 구현한다

각 파일에는 왜 그렇게 했는지 핵심만 주석으로 남기고, 마지막에 "로컬에서 검증하는 명령어"와 "이미지 크기를 더 줄이려면 볼 곳"을 한 줄씩 알려줘.`,
    principle: "재현 가능하고 최소한인 컨테이너 (multi-stage, no secrets in image)",
    why: "멀티스테이지 빌드와 시크릿 분리, 헬스체크는 이미지 크기·보안·배포 안정성을 동시에 좌우하는 기본기입니다. AI에게 이 원칙들을 명시하지 않으면 시크릿이 박히거나 비대한 단일 스테이지 이미지가 나오기 쉽습니다.",
    tip: "compose의 depends_on은 기본적으로 '컨테이너 시작'만 기다립니다. DB가 실제 쿼리를 받을 준비를 기다리려면 condition: service_healthy와 DB healthcheck를 함께 걸어야 합니다.",
    example: `너는 컨테이너 배포 경험이 많은 시니어 DevOps 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

컨테이너화할 앱: 주문을 받는 Express REST API 서버
기술 스택: Node.js 20, Express, Prisma ORM
데이터베이스: PostgreSQL 16
런타임에 필요한 환경변수: DATABASE_URL, JWT_SECRET, PORT

먼저 아래를 계획으로 정리하고, 내가 승인하면 파일을 만들어줘.
1. 만들 파일 목록(Dockerfile, .dockerignore, docker-compose.yml, .env.example)과 각 역할
2. 이미지 빌드 전략과 예상되는 실패 지점(빌드가 느려지는 원인, 캐시 무효화, 이미지 비대화 등)

그다음 아래 원칙을 반드시 지켜서 구현해줘.
- 멀티스테이지 빌드: 빌드 단계와 실행 단계를 분리해 최종 이미지에는 런타임에 필요한 것만 남긴다
- 가능한 한 작은 베이스 이미지(node:20-slim 등)를 쓰고 버전을 고정한다
- 시크릿(비밀번호/토큰)은 절대 이미지에 굽지 않는다. 빌드 인자나 COPY로 넣지 말고 런타임 환경변수로 주입한다
- 컨테이너는 root가 아닌 전용 사용자로 실행한다
- .dockerignore로 node_modules, .git, .env, 테스트 산출물 등을 빌드 컨텍스트에서 제외한다
- HEALTHCHECK로 앱의 실제 준비 상태(GET /health 응답)를 확인한다
- docker-compose에 앱과 postgres를 정의하고, DB 데이터는 named volume으로 영속화하며, 앱은 DB가 healthy가 된 뒤 시작하도록 depends_on 조건을 건다
- 앱은 SIGTERM을 받으면 진행 중 요청을 마치고 Prisma 커넥션을 닫는 graceful shutdown을 구현한다

각 파일에는 왜 그렇게 했는지 핵심만 주석으로 남기고, 마지막에 "로컬에서 검증하는 명령어"와 "이미지 크기를 더 줄이려면 볼 곳"을 한 줄씩 알려줘.`,
    exampleNote: "스택·DB·환경변수 이름이 구체적이라 AI가 Prisma용 멀티스테이지 Dockerfile과 postgres healthcheck가 걸린 compose를 곧장 만들고, 마이그레이션 실행 시점 같은 결정을 되물어옵니다.",
    variations: [
      {
        label: "쿠버네티스 매니페스트 버전",
        prompt: `너는 쿠버네티스 운영 경험이 많은 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

이미 컨테이너 이미지가 있는 앱: [앱/이미지 설명]
연결할 DB: [DB 종류 — 예: 매니지드 PostgreSQL]
필요한 환경변수/시크릿: [이름만 나열]

먼저 배포 구성 계획과 예상 실패 지점(프로브 오설정으로 인한 재시작 루프, 시크릿 노출, 리소스 부족 등)을 정리하고, 승인 후 아래 원칙으로 매니페스트를 만들어줘.
- Deployment: 레플리카 2개 이상, readinessProbe와 livenessProbe를 실제 /health 기준으로 분리해서 설정
- 시크릿은 Secret 리소스로 분리하고 매니페스트에 평문으로 넣지 않는다(예시는 [값] 자리표시)
- ConfigMap으로 비민감 설정을 관리하고, requests/limits로 리소스를 명시한다
- graceful shutdown을 위해 terminationGracePeriod와 preStop, SIGTERM 처리를 맞춘다
- 무중단 배포를 위한 RollingUpdate 전략과 최소 가용 파드 수(PodDisruptionBudget)를 설정한다

마지막에 kubectl로 배포 상태를 확인하는 명령어와 롤백 명령어를 알려줘.`,
      },
      {
        label: "개발용 핫리로드 compose 버전",
        prompt: `너는 개발 생산성을 챙기는 시니어 개발자야. 로컬 개발 전용 docker-compose를 만들어줘. 먼저 계획과 주의점부터.

앱: [앱 설명과 스택]
DB: [DB 종류]

운영용이 아니라 로컬 개발용이라는 점을 전제로, 아래를 지켜서 구성해줘.
- 소스 코드를 bind mount 해서 저장 즉시 반영되는 핫리로드 개발 서버로 앱을 띄운다
- node_modules 같은 의존성 폴더는 호스트와 충돌하지 않게 별도 volume으로 분리한다
- DB는 named volume으로 데이터를 유지하고, 초기 스키마/시드는 자동 실행되게 한다
- 디버깅 포트와 DB 포트를 호스트로 노출하고, 환경변수는 .env로 관리한다(.env.example 제공)
- 운영용 compose와 어떻게 다른지, 이 설정을 운영에 그대로 쓰면 안 되는 이유를 마지막에 3줄로 정리한다

핫리로드가 안 먹히는 흔한 원인(파일 시스템 이벤트 감지 실패 등)과 대처법도 함께 알려줘.`,
      },
    ],
  },
  {
    id: "vibe-ops-02",
    title: "배포 파이프라인과 기본 CI 구성하기",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["배포", "CI/CD", "GitHubActions", "무중단배포"],
    prompt: `너는 CI/CD 파이프라인을 여러 번 구축한 시니어 플랫폼 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

배포할 앱: [앱 설명과 스택]
배포 플랫폼: [Vercel / Railway / Fly.io 중 택1, 모르면 "추천해줘"]
저장소: GitHub, 기본 브랜치는 [main 등]
필요한 시크릿/환경변수: [DB URL, API 키 등 이름만 나열]

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 파이프라인 단계 정의: 언제(push/PR) 무엇을(설치→린트→테스트→빌드→배포) 실행할지
2. 예상되는 실패 지점(시크릿 누락, 테스트 없이 배포, 환경별 설정 혼동, 배포 후에야 드러나는 오류 등)과 각 방지책

그다음 아래 원칙으로 GitHub Actions 워크플로와 배포 설정을 만들어줘.
- PR에서는 린트·테스트·빌드까지만 돌리고(배포는 하지 않음), 기본 브랜치 머지 시에만 실제 배포한다
- 시크릿은 GitHub Actions Secrets와 플랫폼 환경변수로만 주입하고, 코드나 워크플로 파일에 평문으로 넣지 않는다
- 의존성 캐시로 파이프라인을 빠르게 하고, 실행 단계를 나눠 어디서 실패했는지 바로 보이게 한다
- 테스트가 실패하면 배포가 진행되지 않도록 단계 의존성을 건다
- 배포 후 헬스체크(예: /health 200 확인)를 넣고, 실패 시 어떻게 롤백하는지 방법을 문서화한다
- 프리뷰/스테이징과 프로덕션 환경을 구분하는 방법을 명시한다

마지막에 "처음 한 번 손으로 해야 하는 설정(시크릿 등록 등)"을 체크리스트로 주고, 로컬에서 배포를 흉내 내 검증하는 방법을 알려줘.`,
    principle: "테스트를 통과해야만 배포되는 자동 파이프라인 (gated deploy)",
    why: "PR에서 검증하고 머지에서만 배포하도록 게이트를 걸면, 깨진 코드가 프로덕션에 나가는 사고를 구조적으로 막습니다. 시크릿을 저장소 밖에 두는 것도 이 단계에서 굳히는 게 안전합니다.",
    tip: "배포 후 헬스체크가 실패하면 알림을 받도록 설정하세요. '배포 성공'이 곧 '앱 정상'은 아니며, 롤백은 문제를 빨리 알아챌수록 쉬워집니다.",
    example: `너는 CI/CD 파이프라인을 여러 번 구축한 시니어 플랫폼 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

배포할 앱: 파이썬 FastAPI로 만든 REST API 서버
배포 플랫폼: Railway
저장소: GitHub, 기본 브랜치는 main
필요한 시크릿/환경변수: DATABASE_URL, OPENAI_API_KEY

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 파이프라인 단계 정의: 언제(push/PR) 무엇을(설치→ruff 린트→pytest→빌드→배포) 실행할지
2. 예상되는 실패 지점(시크릿 누락, 테스트 없이 배포, 환경별 설정 혼동, 배포 후에야 드러나는 오류 등)과 각 방지책

그다음 아래 원칙으로 GitHub Actions 워크플로와 Railway 배포 설정을 만들어줘.
- PR에서는 ruff·pytest·빌드까지만 돌리고(배포는 하지 않음), main 머지 시에만 Railway로 배포한다
- 시크릿은 GitHub Actions Secrets와 Railway 환경변수로만 주입하고, 코드나 워크플로 파일에 평문으로 넣지 않는다
- pip 의존성 캐시로 파이프라인을 빠르게 하고, 실행 단계를 나눠 어디서 실패했는지 바로 보이게 한다
- pytest가 실패하면 배포가 진행되지 않도록 단계 의존성을 건다
- 배포 후 GET /health 200 확인을 넣고, 실패 시 이전 배포로 롤백하는 방법을 문서화한다
- Railway의 스테이징과 프로덕션 환경을 구분하는 방법을 명시한다

마지막에 "처음 한 번 손으로 해야 하는 설정(Railway 프로젝트 연결, 시크릿 등록 등)"을 체크리스트로 주고, 로컬에서 배포를 흉내 내 검증하는 방법을 알려줘.`,
    exampleNote: "플랫폼(Railway)과 스택(FastAPI)이 정해져 있어 AI가 ruff·pytest 게이트가 걸린 GitHub Actions 워크플로와 Railway 배포 트리거를 바로 작성하고, 스테이징 분리 방식을 되묻습니다.",
    variations: [
      {
        label: "Fly.io 도커 이미지 배포 버전",
        prompt: `너는 Fly.io 운영 경험이 많은 엔지니어야. 코드를 쓰기 전에 계획부터.

배포할 앱: [앱 설명과 스택, Dockerfile 유무]
저장소: GitHub, 기본 브랜치는 [main 등]
필요한 시크릿: [이름만 나열]

먼저 파이프라인 단계와 예상 실패 지점을 정리하고, 승인 후 아래 원칙으로 fly.toml과 GitHub Actions 워크플로를 만들어줘.
- 컨테이너 이미지를 빌드해 Fly에 배포하고, PR에서는 빌드·테스트까지만, 기본 브랜치에서만 flyctl deploy를 실행한다
- 시크릿은 flyctl secrets로 관리하고 이미지나 워크플로에 굽지 않는다
- 무중단 배포를 위해 롤링 배포와 헬스체크(fly.toml의 checks)를 설정하고, 헬스체크 실패 시 자동으로 이전 릴리스를 유지하게 한다
- 리전과 최소 인스턴스 수, 오토스케일 조건을 명시한다
- 배포 실패 시 flyctl로 롤백하는 명령어를 문서화한다

마지막에 최초 1회 설정(fly launch, 토큰 등록)을 체크리스트로 줘.`,
      },
      {
        label: "모노레포 다중 서비스 버전",
        prompt: `너는 모노레포 CI를 설계해본 플랫폼 엔지니어야. 코드를 쓰기 전에 계획부터.

모노레포 구성: [예: apps/web, apps/api, packages/shared]
각 서비스의 배포 대상: [예: web은 Vercel, api는 Railway]
저장소: GitHub, 기본 브랜치는 [main 등]

먼저 파이프라인 전략과 예상 실패 지점(불필요한 전체 재배포, 서비스 간 의존 변경 누락 등)을 정리하고, 승인 후 아래 원칙으로 구현해줘.
- 변경 감지(path filter 또는 turbo/nx의 affected)로 실제 바뀐 서비스만 빌드·테스트·배포한다
- 공유 패키지가 바뀌면 그것에 의존하는 서비스도 함께 검증되도록 의존 그래프를 반영한다
- 서비스별로 배포 대상과 시크릿을 분리하고, 각 배포는 독립적으로 실패/성공하게 한다
- 서비스별 배포 후 헬스체크와 롤백 방법을 각각 문서화한다

마지막에 이 전략이 서비스가 늘어날 때 어떻게 확장되는지 3줄로 정리해줘.`,
      },
    ],
  },
  {
    id: "vibe-ops-03",
    title: "핵심 사용자 흐름을 통합·E2E 테스트로 검증하기",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["통합테스트", "E2E", "테스트자동화", "회귀방지"],
    prompt: `너는 테스트 자동화에 능한 시니어 QA 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

앱: [앱 설명과 스택]
검증하고 싶은 핵심 흐름: [예: 회원가입→로그인→주문 생성→주문 조회처럼 실제 사용자가 밟는 경로]
테스트 도구: [Playwright / Cypress / Supertest 등, 모르면 "추천해줘"]

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 테스트할 핵심 시나리오 목록(각 시나리오의 시작 상태 → 행동 → 검증할 결과)
2. 예상되는 불안정(flaky) 요인과 대처: 네트워크 대기, 시간/랜덤 값, 테스트 간 데이터 오염, 실행 순서 의존 등

그다음 아래 원칙으로 테스트를 작성해줘.
- 구현 세부가 아니라 사용자 관점의 결과를 검증한다(내부 함수 호출이 아니라 화면/응답에 나타나는 결과)
- 각 테스트는 독립적으로 실행되도록 자체적으로 데이터를 준비하고 끝나면 정리한다(테스트 간 순서 의존 금지)
- 고정된 sleep 대신 조건이 충족될 때까지 기다리는 방식으로 flaky를 줄인다
- 테스트 전용 DB나 격리된 환경을 쓰고, 외부 API는 필요한 경우 목/스텁으로 대체한다
- 실패 시 원인을 빨리 알 수 있게 명확한 단언 메시지와(가능하면) 실패 스냅샷/트레이스를 남긴다
- 성공 경로뿐 아니라 대표적인 실패 경로(잘못된 입력, 권한 없음)도 한두 개 포함한다

마지막에 로컬과 CI에서 이 테스트를 실행하는 명령어, 그리고 실행 시간이 길어질 때 우선 병렬화/축소할 지점을 알려줘.`,
    principle: "구현이 아닌 사용자 흐름을 검증하는 안정적 테스트 (test behavior, not internals)",
    why: "핵심 흐름 몇 개를 E2E로 잡아두면 리팩터링이나 배포 때 큰 회귀를 자동으로 막아줍니다. 사용자 관점으로 검증하고 flaky 요인을 처음부터 제거해야 오래 신뢰할 수 있는 테스트가 됩니다.",
    tip: "모든 것을 E2E로 덮으려 하지 마세요. 느리고 깨지기 쉽습니다. '이게 깨지면 서비스가 망가진다' 싶은 흐름 3~5개만 E2E로, 나머지는 더 빠른 통합/단위 테스트로 분담하는 게 좋습니다.",
    example: `너는 테스트 자동화에 능한 시니어 QA 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

앱: Next.js로 만든 온라인 쇼핑몰(프론트+API 라우트)
검증하고 싶은 핵심 흐름: 방문자가 회원가입하고 로그인한 뒤, 상품을 장바구니에 담고 주문을 생성하면, 주문 내역 페이지에 그 주문이 보이는 것까지
테스트 도구: Playwright

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 테스트할 핵심 시나리오 목록(각 시나리오의 시작 상태 → 행동 → 검증할 결과)
2. 예상되는 불안정(flaky) 요인과 대처: 네트워크 대기, 시간/랜덤 값, 테스트 간 데이터 오염, 실행 순서 의존 등

그다음 아래 원칙으로 테스트를 작성해줘.
- 구현 세부가 아니라 사용자 관점의 결과를 검증한다(내부 함수 호출이 아니라 화면에 나타나는 결과)
- 각 테스트는 독립적으로 실행되도록 자체적으로 계정/데이터를 준비하고 끝나면 정리한다(테스트 간 순서 의존 금지)
- 고정된 sleep 대신 요소가 나타날 때까지 기다리는 Playwright의 자동 대기를 활용해 flaky를 줄인다
- 테스트 전용 DB를 쓰고, 결제 같은 외부 API는 목/스텁으로 대체한다
- 실패 시 원인을 빨리 알 수 있게 명확한 단언 메시지와 실패 스크린샷/트레이스를 남긴다
- 성공 경로뿐 아니라 실패 경로(잘못된 비밀번호로 로그인 실패, 재고 없는 상품 주문 시도)도 한두 개 포함한다

마지막에 로컬과 CI에서 이 테스트를 실행하는 명령어, 그리고 실행 시간이 길어질 때 우선 병렬화/축소할 지점을 알려줘.`,
    exampleNote: "핵심 흐름이 '가입→로그인→주문→조회'로 못박혀 있어 AI가 계정 격리와 자동 대기를 갖춘 Playwright 시나리오를 바로 짜고, 결제 API를 어떻게 목으로 대체할지 되묻습니다.",
    variations: [
      {
        label: "API 통합테스트(Supertest) 버전",
        prompt: `너는 백엔드 테스트에 능한 시니어 개발자야. 코드를 쓰기 전에 계획부터.

API 서버: [스택과 주요 엔드포인트]
검증할 핵심 흐름: [예: 리소스 생성→조회→수정→삭제와 권한 검사]

먼저 시나리오 목록과 flaky/격리 위험을 정리하고, 승인 후 아래 원칙으로 HTTP 레벨 통합 테스트를 작성해줘.
- 실제 라우팅·미들웨어·DB를 거치는 HTTP 요청으로 검증한다(핸들러 함수 직접 호출이 아니라 요청→응답)
- 각 테스트는 자체 데이터를 준비하고 트랜잭션 롤백이나 정리로 서로 오염되지 않게 한다
- 상태코드, 응답 본문, 그리고 DB에 실제로 반영됐는지(부수효과)를 함께 단언한다
- 인증/권한 경로: 토큰 없음, 만료 토큰, 타인 리소스 접근 같은 실패 케이스를 포함한다
- 외부 서비스는 목으로 대체하고, 테스트 DB는 격리한다

마지막에 CI에서 DB를 띄우고 이 테스트를 실행하는 방법을 알려줘.`,
      },
      {
        label: "CI 통합 + 커버리지 게이트 버전",
        prompt: `너는 테스트를 CI에 녹여본 플랫폼 엔지니어야. 코드를 쓰기 전에 계획부터.

기존 테스트: [현재 테스트 도구와 대략적 범위]
CI: GitHub Actions

먼저 파이프라인에 테스트를 넣는 전략과 예상 실패 지점(서비스 컨테이너 기동 실패, flaky로 인한 랜덤 실패, 느린 실행 등)을 정리하고, 승인 후 아래 원칙으로 워크플로를 만들어줘.
- PR마다 통합/E2E 테스트를 실행하고, 실패하면 머지를 막는다
- 테스트에 필요한 DB 등은 서비스 컨테이너로 CI 안에서 띄운다
- flaky 대응: 재시도는 최소화하고, 반복 실패하는 테스트를 눈에 띄게 리포트한다
- 커버리지를 측정하되 과한 목표 대신 핵심 흐름이 덮였는지에 초점을 둔 합리적 기준선을 둔다
- E2E 실패 시 스크린샷/트레이스를 아티팩트로 업로드해 디버깅을 돕는다

마지막에 실행 시간이 병목일 때 병렬화·샤딩하는 방법을 알려줘.`,
      },
    ],
  },
  {
    id: "vibe-ops-04",
    title: "구조적 로깅과 에러 트래킹 붙이기",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["구조적로그", "에러트래킹", "Sentry", "관측성"],
    prompt: `너는 운영 관측성(observability)을 챙기는 시니어 백엔드 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

앱: [앱 설명과 스택]
현재 로깅 상태: [예: 그냥 print/console.log만 씀]
에러 모니터링: [Sentry 등 쓰고 싶은 도구, 모르면 "추천해줘"]

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 어떤 이벤트를 어떤 레벨(debug/info/warn/error)로 남길지 기준
2. 예상되는 함정: 민감정보(비밀번호·토큰·개인정보) 로그 유출, 로그 폭증, 요청을 가로질러 추적이 안 되는 문제 등과 각 대처

그다음 아래 원칙으로 구현해줘.
- 사람이 읽는 문자열이 아니라 기계가 파싱 가능한 구조적 JSON 로그로 남긴다(타임스탬프, 레벨, 메시지, 컨텍스트 필드)
- 각 요청에 요청 ID(correlation id)를 부여해 한 요청에서 나온 로그를 한 줄기로 추적할 수 있게 한다
- 민감정보는 로깅 전에 마스킹/제외한다. 무엇을 절대 남기지 않을지 명시적으로 정의한다
- 로그 레벨을 환경변수로 제어하고(개발은 상세, 운영은 info 이상), 운영에서는 과도한 debug 로그를 끈다
- 처리되지 않은 예외와 프로미스 거부를 한 곳에서 잡아 에러 트래킹 도구로 보고하고, 요청 컨텍스트(사용자·경로·요청 ID)를 함께 첨부한다
- 에러 트래킹 DSN/키는 환경변수로 주입하고 코드에 넣지 않는다. 릴리스/환경 태그를 달아 배포별로 구분한다

마지막에 로그가 실제로 잘 나오는지 로컬에서 확인하는 방법과, 알림을 어떤 조건에서 받도록 설정하면 좋을지 제안해줘.`,
    principle: "기계가 읽는 구조적 로그 + 중앙 집중 에러 추적 (structured logs, tracked errors)",
    why: "문자열 로그는 사람이 하나씩 읽어야 하지만 구조적 JSON 로그는 검색·집계·알림이 가능합니다. 요청 ID와 에러 트래킹을 함께 붙이면 장애가 났을 때 '언제, 누구에게, 어디서' 무슨 일이 있었는지 몇 분 안에 재구성할 수 있습니다.",
    tip: "로그에 무엇을 남기지 '않을지'를 먼저 정하세요. 비밀번호·토큰·카드번호·주민번호가 로그에 새어 나가는 사고는 흔하고, 한번 쌓인 로그에서 지우기는 매우 번거롭습니다.",
    example: `너는 운영 관측성(observability)을 챙기는 시니어 백엔드 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

앱: Node.js Express로 만든 결제 API 서버
현재 로깅 상태: 여기저기 console.log만 흩어져 있음
에러 모니터링: Sentry

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 어떤 이벤트를 어떤 레벨(debug/info/warn/error)로 남길지 기준
2. 예상되는 함정: 민감정보(비밀번호·토큰·카드정보) 로그 유출, 로그 폭증, 요청을 가로질러 추적이 안 되는 문제 등과 각 대처

그다음 아래 원칙으로 구현해줘.
- console.log 대신 pino로 구조적 JSON 로그를 남긴다(타임스탬프, 레벨, 메시지, 컨텍스트 필드)
- 각 요청에 요청 ID(correlation id)를 부여하는 미들웨어를 두고, 그 요청에서 나온 모든 로그에 자동으로 붙인다
- 카드정보·토큰·비밀번호는 로깅 전에 마스킹/제외한다. 절대 남기지 않을 필드 목록을 명시한다
- 로그 레벨을 LOG_LEVEL 환경변수로 제어하고(개발은 debug, 운영은 info 이상), 운영에서 과도한 debug 로그를 끈다
- 처리되지 않은 예외와 프로미스 거부를 한 곳에서 잡아 Sentry로 보고하고, 요청 컨텍스트(사용자·경로·요청 ID)를 함께 첨부한다
- SENTRY_DSN은 환경변수로 주입하고 코드에 넣지 않는다. 릴리스/환경 태그를 달아 배포별로 구분한다

마지막에 로그가 실제로 잘 나오는지 로컬에서 확인하는 방법과, 알림을 어떤 조건에서 받도록 설정하면 좋을지 제안해줘.`,
    exampleNote: "결제 API라는 맥락 덕에 AI가 카드정보 마스킹을 최우선으로 잡고, pino 구조적 로그 + 요청 ID 미들웨어 + Sentry 연동을 컨텍스트 첨부까지 갖춰 구성합니다.",
    variations: [
      {
        label: "파이썬 structlog 버전",
        prompt: `너는 파이썬 서비스 관측성에 능한 엔지니어야. 코드를 쓰기 전에 계획부터.

앱: [파이썬 스택 — 예: FastAPI]
에러 모니터링: [Sentry 등]

먼저 로그 레벨 기준과 함정(민감정보 유출, 로그 폭증, 요청 추적 불가)을 정리하고, 승인 후 아래 원칙으로 구현해줘.
- structlog로 구조적 JSON 로그를 남기고, 표준 logging과도 자연스럽게 연동한다
- 요청 미들웨어에서 요청 ID를 contextvar로 바인딩해 같은 요청의 로그가 한 줄기로 묶이게 한다
- 민감 필드는 프로세서 단계에서 마스킹하고, 로그 레벨은 환경변수로 제어한다
- 처리되지 않은 예외는 전역 핸들러에서 잡아 에러 트래킹 도구로 보고하고 요청 컨텍스트를 첨부한다
- DSN 등 시크릿은 환경변수로 주입하고 릴리스/환경 태그를 단다

마지막에 로컬에서 JSON 로그를 사람이 읽기 좋게 보는 방법을 알려줘.`,
      },
      {
        label: "프론트엔드 에러 트래킹 버전",
        prompt: `너는 프론트엔드 운영을 챙기는 엔지니어야. 코드를 쓰기 전에 계획부터.

앱: [프론트 스택 — 예: React SPA]
에러 모니터링: [Sentry 등]

먼저 무엇을 수집하고 무엇을 수집하면 안 되는지(개인정보) 기준을 정하고, 승인 후 아래 원칙으로 구현해줘.
- 전역 에러/프로미스 거부와 렌더링 에러(Error Boundary)를 모두 포착해 트래킹 도구로 보고한다
- 소스맵을 업로드하되 배포 산출물에는 노출하지 않아, 압축된 코드에서도 원본 위치를 추적할 수 있게 한다
- 사용자 식별은 개인정보 없이 익명 ID 수준으로 하고, 입력값·토큰 등 민감정보는 이벤트에서 제거한다
- 릴리스 버전과 환경 태그를 달아 어느 배포에서 난 에러인지 구분한다
- 에러 폭증 시 샘플링/속도 제한으로 노이즈와 비용을 관리한다

마지막에 실제 에러를 일부러 발생시켜 트래킹이 되는지 검증하는 방법을 알려줘.`,
      },
    ],
  },
  {
    id: "vibe-ops-05",
    title: "레이트 리미팅과 기본 보안 다지기",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["레이트리밋", "보안헤더", "CORS", "입력검증"],
    prompt: `너는 웹 보안에 밝은 시니어 백엔드 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

앱: [앱 설명과 스택]
공개 형태: [예: 브라우저 프론트가 붙는 공개 API]
허용할 출처(도메인): [프론트 도메인들, 없으면 "미정"]
민감/비용 발생 엔드포인트: [예: 로그인, 회원가입, 결제, 외부 API 호출 등]

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 위협 표면 정리: 어떤 엔드포인트가 남용/공격에 취약한지와 이유
2. 예상되는 함정: 과도하게 열린 CORS, 검증 없는 입력, 레이트리밋이 프록시 뒤에서 IP를 잘못 잡는 문제 등과 각 대처

그다음 아래 원칙으로 구현해줘.
- 입력 검증: 모든 외부 입력(본문·쿼리·헤더)을 스키마로 검증하고, 통과하지 못하면 명확한 에러로 거부한다. 화이트리스트 방식으로 허용된 필드만 받는다
- CORS: 아무 출처나 허용(*)하지 말고 허용 도메인 목록만 명시한다. 자격증명 허용 여부를 신중히 정한다
- 보안 헤더: 기본 보안 헤더(콘텐츠 타입 스니핑 방지, 프레임 차단, HSTS 등)를 적용한다
- 레이트 리미팅: 엔드포인트별로 한도를 다르게 둔다(로그인 같은 민감 경로는 더 엄격하게). 프록시 뒤에서는 신뢰할 수 있는 클라이언트 IP를 정확히 식별하도록 설정한다
- 남용 방지: 로그인 실패 누적 시 지연/차단, 페이로드 크기 제한, 무거운 작업에 대한 별도 제한을 둔다
- 에러 응답에 내부 구조(스택트레이스, 쿼리 등)를 노출하지 않는다

마지막에 각 방어가 실제로 동작하는지(예: 한도 초과 시 429, 잘못된 입력 시 400) 확인하는 방법을 알려주고, 절대 하지 말아야 할 것 3가지를 정리해줘.`,
    principle: "모든 외부 입력을 의심하고 남용을 구조적으로 제한하기 (validate, restrict, rate-limit)",
    why: "공개된 엔드포인트는 반드시 오·남용됩니다. 입력 검증·CORS·보안 헤더·레이트리밋은 대부분의 흔한 공격과 폭주를 값싸게 막아주는 기본 방어선이며, 사고가 난 뒤보다 처음 붙일 때 훨씬 저렴합니다.",
    tip: "레이트리밋을 리버스 프록시/로드밸런서 뒤에 두면 모든 요청이 프록시 IP 하나로 보일 수 있습니다. 신뢰할 프록시를 지정하고 전달된 클라이언트 IP를 정확히 읽도록 설정해야 한도가 제대로 걸립니다.",
    example: `너는 웹 보안에 밝은 시니어 백엔드 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

앱: Node.js Express로 만든 공개 REST API
공개 형태: 별도 도메인의 React 프론트가 붙는 공개 API
허용할 출처(도메인): https://app.example.com
민감/비용 발생 엔드포인트: POST /auth/login, POST /auth/signup, POST /ai/generate(외부 LLM 호출로 비용 발생)

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 위협 표면 정리: 어떤 엔드포인트가 남용/공격에 취약한지와 이유
2. 예상되는 함정: 과도하게 열린 CORS, 검증 없는 입력, 레이트리밋이 프록시 뒤에서 IP를 잘못 잡는 문제 등과 각 대처

그다음 아래 원칙으로 구현해줘.
- 입력 검증: 모든 외부 입력(본문·쿼리·헤더)을 zod 스키마로 검증하고, 통과하지 못하면 400으로 거부한다. 화이트리스트 방식으로 허용된 필드만 받는다
- CORS: 아무 출처나 허용(*)하지 말고 https://app.example.com만 명시한다. 자격증명 허용 여부를 신중히 정한다
- 보안 헤더: helmet으로 기본 보안 헤더(콘텐츠 타입 스니핑 방지, 프레임 차단, HSTS 등)를 적용한다
- 레이트 리미팅: 엔드포인트별로 한도를 다르게 둔다. /auth/login과 /ai/generate는 더 엄격하게 건다. 프록시 뒤에서는 신뢰할 수 있는 클라이언트 IP를 정확히 식별하도록 trust proxy를 설정한다
- 남용 방지: 로그인 실패 누적 시 지연/차단, 페이로드 크기 제한, /ai/generate에 사용자별 별도 제한을 둔다
- 에러 응답에 내부 구조(스택트레이스, 쿼리 등)를 노출하지 않는다

마지막에 각 방어가 실제로 동작하는지(예: 한도 초과 시 429, 잘못된 입력 시 400) 확인하는 방법을 알려주고, 절대 하지 말아야 할 것 3가지를 정리해줘.`,
    exampleNote: "비용이 드는 /ai/generate와 인증 경로가 명시돼 있어 AI가 엔드포인트별 차등 레이트리밋, trust proxy 설정, zod 검증, helmet + 좁은 CORS를 우선순위대로 배치합니다.",
    variations: [
      {
        label: "엣지/프록시 레벨 레이트리밋 버전",
        prompt: `너는 인프라 레벨 방어를 설계하는 엔지니어야. 코드를 쓰기 전에 계획부터.

앱 앞단: [예: Nginx 리버스 프록시 / Cloudflare 등]
백엔드: [스택]
보호할 경로: [경로들]

먼저 앱 레벨과 엣지 레벨 방어를 어떻게 나눌지, 각 함정(정상 사용자 오차단, 프록시 IP 오인식 등)을 정리하고, 승인 후 아래 원칙으로 구성해줘.
- 엣지/프록시에서 IP·경로 단위 레이트리밋과 커넥션 제한, 요청 크기 제한을 건다
- 봇/폭주 트래픽을 앱에 닿기 전에 걸러내되, 정상 사용자를 오차단하지 않도록 한도를 데이터로 정한다
- 앱은 프록시가 전달한 실제 클라이언트 IP를 신뢰 설정에 맞춰 읽는다
- 엣지 방어와 앱 방어가 중복·상충하지 않게 역할을 나눈다
- 차단 발생 시 관측 가능하도록 로깅/지표를 남긴다

마지막에 부하 테스트로 한도를 검증하고 튜닝하는 방법을 알려줘.`,
      },
      {
        label: "인증 엔드포인트 남용 방지 집중 버전",
        prompt: `너는 계정 보안을 다뤄본 엔지니어야. 코드를 쓰기 전에 계획부터.

인증 방식: [예: 이메일+비밀번호, OTP 등]
스택: [백엔드 스택]

먼저 인증 경로의 위협(무차별 대입, 자격증명 스터핑, 계정 열거, OTP 남용)을 정리하고, 승인 후 아래 원칙으로 방어를 구현해줘.
- 로그인/OTP 요청에 IP와 계정 기준의 이중 레이트리밋을 걸고, 실패 누적 시 점증 지연(backoff)이나 일시 잠금을 적용한다
- 존재하는 계정과 없는 계정의 응답·응답시간을 동일하게 만들어 계정 열거를 막는다
- 비밀번호는 강력한 해시(bcrypt/argon2)로 저장하고, 흔한/유출된 비밀번호를 거부한다
- OTP·재설정 토큰은 짧은 만료·1회용으로 하고 재발급 빈도를 제한한다
- 의심스러운 시도는 로깅/알림으로 관측 가능하게 한다

마지막에 실제 사용자를 막지 않으면서 공격만 걸러지는지 검증하는 방법을 알려줘.`,
      },
    ],
  },
  {
    id: "vibe-ops-06",
    title: "WebSocket/SSE로 실시간 알림·업데이트 구현하기",
    category: "vibe-coding",
    difficulty: "고급",
    tags: ["실시간", "WebSocket", "SSE", "알림"],
    prompt: `너는 실시간 시스템을 구축해본 시니어 백엔드 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

앱: [앱 설명과 스택]
실시간으로 전달할 것: [예: 새 알림, 주문 상태 변경, 라이브 대시보드 수치]
전달 방향: [서버→클라 단방향 / 양방향 중 어느 것]
예상 동시 접속 규모: [대략, 모르면 "미정"]

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 기술 선택: 단방향이면 SSE, 양방향/저지연이면 WebSocket 중 무엇이 적절한지와 이유
2. 예상되는 실패 모드: 연결 끊김·재접속, 놓친 메시지, 인증 안 된 구독, 브로드캐스트 폭증, 여러 서버 인스턴스 간 메시지 전파 안 됨 등과 각 대처

그다음 아래 원칙으로 구현해줘.
- 연결 시점에 인증/인가를 검증하고, 사용자는 자신이 볼 권한이 있는 채널/이벤트만 구독하게 한다
- 클라이언트는 연결이 끊기면 지수 백오프로 자동 재접속하고, 재접속 시 놓친 메시지를 복구할 방법(마지막 이벤트 ID 등)을 둔다
- 서버는 죽은 연결을 감지하도록 heartbeat/ping을 두고, 응답 없는 연결을 정리한다
- 서버 인스턴스가 여러 개로 늘어날 것을 대비해, 인스턴스 간 이벤트 전파(예: Redis pub/sub 같은 브로커) 구조를 설계에 반영한다
- 메시지 폭증에 대비해 전송 속도/버퍼를 제어하고, 느린 소비자가 서버를 막지 않게 한다
- 종료 시 graceful shutdown으로 열린 연결을 정리하고 진행 중 이벤트를 흘리지 않는다

마지막에 로컬에서 여러 클라이언트로 실시간 동작과 재접속을 검증하는 방법, 그리고 폴링 대비 이 방식의 트레이드오프를 정리해줘.`,
    principle: "끊겨도 복구되는 실시간 연결 (auth on connect, reconnect, cross-instance fanout)",
    why: "실시간 기능의 난이도는 '메시지를 보내는 것'이 아니라 '끊김·중복·다중 서버'를 다루는 데 있습니다. 연결 인증, 자동 재접속, 놓친 메시지 복구, 인스턴스 간 전파를 처음부터 설계에 넣어야 규모가 커져도 무너지지 않습니다.",
    tip: "실시간이 정말 필요한지 먼저 따져보세요. 몇 초의 지연이 허용된다면 단순 폴링이나 SSE가 WebSocket보다 운영이 훨씬 쉽습니다. 양방향·저지연이 꼭 필요할 때만 WebSocket을 택하세요.",
    example: `너는 실시간 시스템을 구축해본 시니어 백엔드 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

앱: Node.js Express로 만든 협업 할 일 앱
실시간으로 전달할 것: 같은 워크스페이스의 다른 사람이 할 일을 추가/완료하면 내 화면에도 즉시 반영, 그리고 나에게 온 멘션 알림
전달 방향: 서버→클라가 주지만, 클라가 구독 채널을 바꾸는 양방향도 필요
예상 동시 접속 규모: 초기엔 수백, 이후 수천까지 성장 가능

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 기술 선택: 이 요구에는 WebSocket(Socket.IO)이 적절한지와 이유
2. 예상되는 실패 모드: 연결 끊김·재접속, 놓친 업데이트, 인증 안 된 구독, 브로드캐스트 폭증, 여러 서버 인스턴스 간 메시지 전파 안 됨 등과 각 대처

그다음 아래 원칙으로 구현해줘.
- 연결 시점에 JWT로 인증하고, 사용자는 자신이 속한 워크스페이스 채널만 구독하게 한다
- 클라이언트는 연결이 끊기면 지수 백오프로 자동 재접속하고, 재접속 시 마지막 수신 시점 이후 변경을 다시 받아 놓친 업데이트를 복구한다
- 서버는 heartbeat/ping으로 죽은 연결을 감지해 정리한다
- 서버 인스턴스가 여러 개로 늘어날 것을 대비해, 인스턴스 간 이벤트 전파를 Redis pub/sub 어댑터로 구성한다
- 브로드캐스트 폭증에 대비해 전송 속도를 제어하고, 느린 소비자가 서버를 막지 않게 한다
- 종료 시 graceful shutdown으로 열린 소켓을 정리한다

마지막에 로컬에서 브라우저 탭 여러 개로 실시간 동기화와 재접속을 검증하는 방법, 그리고 폴링 대비 이 방식의 트레이드오프를 정리해줘.`,
    exampleNote: "'수백→수천'으로 성장한다는 규모가 명시돼 AI가 처음부터 Redis pub/sub 다중 인스턴스 전파와 워크스페이스 단위 인가, 재접속 복구를 갖춘 Socket.IO 설계를 제시합니다.",
    variations: [
      {
        label: "SSE 단방향 알림 버전",
        prompt: `너는 실시간 알림을 가볍게 구현하려는 엔지니어야. 코드를 쓰기 전에 계획부터.

앱: [스택]
전달할 것: [서버→클라 단방향 알림/업데이트]

양방향이 필요 없다는 점을 살려, 승인 후 아래 원칙으로 SSE(Server-Sent Events)로 구현해줘.
- 서버는 text/event-stream으로 이벤트를 보내고, 각 이벤트에 ID를 붙인다
- 클라이언트는 EventSource로 구독하고, 끊기면 자동 재연결하며 Last-Event-ID로 놓친 이벤트를 이어받는다
- 연결 시 인증하고, 사용자는 자신에게 허용된 이벤트만 받는다
- 주기적 코멘트/heartbeat로 프록시의 유휴 연결 종료를 방지한다
- 다중 인스턴스에서도 특정 사용자에게 이벤트가 닿도록 브로커로 전파한다

마지막에 SSE가 WebSocket 대비 유리한 점과 한계(단방향, 연결 수 제한 등)를 정리하고 검증 방법을 알려줘.`,
      },
      {
        label: "Redis pub/sub 스케일아웃 집중 버전",
        prompt: `너는 실시간 서비스를 수평 확장해본 엔지니어야. 코드를 쓰기 전에 계획부터.

현재 상태: [단일 인스턴스 WebSocket 서버가 이미 있음 — 스택 명시]
목표: 인스턴스를 여러 개로 늘려도 모든 사용자가 올바른 이벤트를 받게 하기

먼저 단일 인스턴스에서 다중 인스턴스로 갈 때 깨지는 지점(다른 서버에 붙은 사용자에게 메시지가 안 감, 중복 전송, 세션 고정 필요 여부)을 정리하고, 승인 후 아래 원칙으로 구현해줘.
- 인스턴스 간 이벤트를 Redis pub/sub(또는 유사 브로커)로 전파해, 어느 서버에 붙었든 대상 사용자에게 도달하게 한다
- 채널/룸 개념으로 필요한 인스턴스에만 이벤트가 가도록 하고, 중복 전송을 막는다
- 로드밸런서 설정(스티키 세션 필요 여부)과 브로커 장애 시 동작을 정의한다
- 브로커 지연/장애를 관측할 지표와 재연결 전략을 둔다

마지막에 인스턴스를 2개 이상 띄워 교차 전파가 되는지 로컬에서 검증하는 방법을 알려줘.`,
      },
    ],
  },
  {
    id: "vibe-ops-07",
    title: "잡 큐와 스케줄러로 백그라운드 작업 처리하기",
    category: "vibe-coding",
    difficulty: "고급",
    tags: ["잡큐", "스케줄러", "cron", "비동기작업"],
    prompt: `너는 비동기 작업 처리 시스템을 설계해본 시니어 백엔드 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

앱: [앱 설명과 스택]
백그라운드로 빼고 싶은 작업: [예: 이메일 발송, 이미지 처리, 리포트 생성 같은 무겁거나 느린 작업]
주기적으로 돌릴 작업: [예: 매일 밤 집계, 만료 데이터 정리 등, 없으면 "없음"]

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 어떤 작업을 요청-응답에서 분리해 큐로 넘기고, 어떤 작업을 스케줄(cron)로 돌릴지 구분
2. 예상되는 실패 모드: 작업 실패·재시도 폭주, 중복 실행, 유실, 워커가 죽었을 때 진행 중 작업 처리, 크론 중복 실행 등과 각 대처

그다음 아래 원칙으로 구현해줘.
- 잡 큐를 도입해 무거운 작업을 요청 처리에서 분리하고, 전용 워커 프로세스가 소비하게 한다
- 실패한 작업은 지수 백오프로 제한된 횟수만 재시도하고, 끝내 실패한 작업은 dead-letter(실패 큐)로 보내 유실 없이 조사할 수 있게 한다
- 작업은 멱등(idempotent)하게 설계해 같은 작업이 두 번 실행돼도 결과가 안전하게 한다
- 워커는 SIGTERM 시 graceful shutdown으로 진행 중 작업을 마치거나 안전히 반환하고 새 작업을 받지 않는다
- 스케줄 작업은 여러 인스턴스가 있어도 한 번만 실행되도록 잠금/단일 스케줄러를 둔다
- 큐 적체, 실패율, 처리 지연을 관측할 수 있게 로깅/지표를 남기고, 임계치 초과 시 알림한다

마지막에 로컬에서 작업 적재→처리→재시도→실패 큐 이동을 검증하는 방법과, 트래픽이 늘 때 워커를 어떻게 확장하는지 알려줘.`,
    principle: "느린 일은 큐로 분리하고, 실패·중복을 견디게 설계하기 (async jobs: retries, idempotency, dead-letter)",
    why: "무거운 작업을 요청-응답 안에서 처리하면 응답이 느려지고 타임아웃과 유실이 생깁니다. 잡 큐로 분리하고 재시도·멱등성·dead-letter·단일 크론을 갖추면, 실패해도 유실 없이 복구되는 견고한 비동기 처리가 됩니다.",
    tip: "재시도는 반드시 백오프와 최대 횟수를 함께 두세요. 실패한 작업을 즉시·무한 재시도하면 장애가 났을 때 큐가 폭주해 시스템 전체를 더 망가뜨립니다.",
    example: `너는 비동기 작업 처리 시스템을 설계해본 시니어 백엔드 엔지니어야. 코드를 쓰기 전에 계획부터 세워줘.

앱: Node.js Express로 만든 뉴스레터 서비스
백그라운드로 빼고 싶은 작업: 구독자 수만 명에게 이메일 발송(외부 메일 API 호출, 느리고 실패 가능)
주기적으로 돌릴 작업: 매일 오전 8시에 그날 보낼 뉴스레터를 예약 발송 큐에 적재, 매주 만료된 미인증 계정 정리

먼저 아래를 계획으로 정리하고, 승인하면 구현해줘.
1. 어떤 작업을 요청-응답에서 분리해 큐로 넘기고, 어떤 작업을 스케줄(cron)로 돌릴지 구분
2. 예상되는 실패 모드: 메일 발송 실패·재시도 폭주, 같은 구독자에게 중복 발송, 작업 유실, 워커가 죽었을 때 진행 중 작업 처리, 크론 중복 실행 등과 각 대처

그다음 아래 원칙으로 구현해줘.
- BullMQ와 Redis로 잡 큐를 구성해 이메일 발송을 요청 처리에서 분리하고, 전용 워커가 소비하게 한다
- 실패한 발송은 지수 백오프로 제한된 횟수만 재시도하고, 끝내 실패하면 실패 큐로 보내 유실 없이 조사할 수 있게 한다
- 발송 작업은 멱등하게 설계해(구독자+뉴스레터 단위 키) 같은 사람에게 두 번 보내지지 않게 한다
- 워커는 SIGTERM 시 graceful shutdown으로 진행 중 발송을 마치고 새 작업을 받지 않는다
- 매일 오전 8시 적재와 주간 정리 같은 스케줄 작업은 인스턴스가 여러 개여도 한 번만 실행되도록 한다
- 큐 적체, 발송 실패율, 처리 지연을 관측할 수 있게 로깅/지표를 남기고, 임계치 초과 시 알림한다

마지막에 로컬에서 발송 적재→처리→재시도→실패 큐 이동을 검증하는 방법과, 발송량이 늘 때 워커를 어떻게 확장하는지 알려줘.`,
    exampleNote: "'수만 명 이메일 발송 + 매일/매주 크론'이라는 맥락 덕에 AI가 멱등 키로 중복 발송을 막고, 백오프 재시도와 실패 큐, 단일 스케줄러를 갖춘 BullMQ 구조를 제시합니다.",
    variations: [
      {
        label: "서버리스 크론 버전",
        prompt: `너는 서버리스 환경의 주기 작업을 설계하는 엔지니어야. 코드를 쓰기 전에 계획부터.

플랫폼: [예: Vercel Cron / Cloudflare Workers Cron / AWS EventBridge 등]
주기 작업: [무엇을 얼마나 자주]

상시 워커를 둘 수 없는 서버리스라는 점을 전제로, 승인 후 아래 원칙으로 구현해줘.
- 크론 트리거가 호출하는 엔드포인트/핸들러를 만들고, 외부가 임의로 호출하지 못하게 시크릿으로 보호한다
- 실행 시간 제한(타임아웃) 안에 끝나도록 작업을 배치/분할하고, 남은 일은 다음 실행이나 큐로 넘긴다
- 크론이 겹쳐 두 번 실행돼도 안전하도록 멱등하게 설계하고, 필요하면 실행 잠금을 둔다
- 실패 시 재시도 정책과 알림을 정의하고, 각 실행 결과를 로깅한다
- 오래 걸리는 작업은 큐(예: 매니지드 큐)에 넘기고 크론은 트리거 역할만 하게 한다

마지막에 크론이 실제로 도는지, 실패 시 알림이 오는지 검증하는 방법을 알려줘.`,
      },
      {
        label: "파이썬 Celery 버전",
        prompt: `너는 파이썬 비동기 작업 처리에 능한 엔지니어야. 코드를 쓰기 전에 계획부터.

앱: [파이썬 스택 — 예: Django/FastAPI]
백그라운드 작업: [무거운 작업들]
주기 작업: [있으면 — Celery beat로]

먼저 큐로 뺄 작업과 스케줄 작업을 구분하고 실패 모드를 정리한 뒤, 승인 후 아래 원칙으로 Celery로 구현해줘.
- 브로커(Redis/RabbitMQ)와 워커를 구성해 무거운 작업을 태스크로 분리한다
- 태스크에 지수 백오프 재시도와 최대 횟수, 최종 실패 처리(dead-letter 상당)를 둔다
- 태스크를 멱등하게 설계하고, acks_late와 워커 프리페치 설정으로 워커 사망 시 작업 유실/중복을 제어한다
- 주기 작업은 Celery beat로 정의하되 다중 인스턴스에서 중복 실행되지 않게 한다
- 큐 길이·실패율·지연을 관측할 지표와 알림을 둔다

마지막에 로컬에서 워커·beat를 띄워 작업 처리와 재시도를 검증하는 방법을 알려줘.`,
      },
    ],
  },
];
