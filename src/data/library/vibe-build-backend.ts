import type { LibraryPrompt } from '../types';

export const prompts: LibraryPrompt[] = [
  {
    id: "vibe-be-01",
    title: "요구사항에서 DB 스키마 설계하고 마이그레이션까지",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["DB스키마", "정규화", "데이터모델링", "마이그레이션"],
    prompt: `너는 데이터 모델링에 밝은 시니어 백엔드 개발자야. 아직 코드나 SQL은 쓰지 마.

만들 것: [무엇을 저장하는 서비스인지 한두 문장]
핵심 요구사항: [저장·조회해야 하는 데이터와 관계 — 예: 사용자가 여러 주문을 갖고, 주문은 여러 상품을 포함]
기술 스택/DB: [예: PostgreSQL, 정했으면 ORM도. 모르면 "추천해줘"]

아래 순서로 진행해줘.
1. 요구사항에서 찾아낸 엔티티(개체)와 그들 사이의 관계(1:N, N:M)를 목록으로 정리하고, 내가 놓쳤을 법한 관계를 되물어봐
2. 최소 3정규형(3NF)을 기준으로 테이블 스키마를 설계해 — 각 테이블의 컬럼·타입·제약조건(PK, FK, UNIQUE, NOT NULL, DEFAULT)을 표로 정리해
   - N:M 관계는 조인 테이블로 분리하고, 금액은 정수(최소 화폐 단위)나 DECIMAL로, 시각은 타임존 포함 타입으로
   - 소프트 삭제 여부와 공통 컬럼(created_at, updated_at) 정책도 정해줘
3. 자주 쓰일 조회를 예상해 필요한 인덱스를 제안하고, 각 인덱스가 어떤 쿼리를 위한 것인지 한 줄로
4. 위 설계를 그대로 반영하는 마이그레이션(DDL 또는 ORM 마이그레이션 파일)을 작성해
5. 의도적으로 비정규화한 부분이 있으면 그 이유를 표시해

먼저 1~3번(설계)까지만 주고, 내 승인 뒤에 4번 마이그레이션을 작성해.`,
    principle: "스키마 우선 설계 (정규화 + 마이그레이션)",
    why: "테이블 구조는 나중에 바꾸기 가장 비싼 결정이라, 코드를 짜기 전에 엔티티·관계·제약조건을 먼저 확정하면 데이터 무결성 문제와 대규모 재설계를 피할 수 있습니다.",
    tip: "설계 단계에서 '이 데이터를 어떻게 조회할지'를 함께 물으면 인덱스와 비정규화 판단이 훨씬 정확해집니다.",
    example: `너는 데이터 모델링에 밝은 시니어 백엔드 개발자야. 아직 코드나 SQL은 쓰지 마.

만들 것: 소규모 쇼핑몰의 주문 관리 백엔드. 고객이 상품을 담아 주문하면 주문 내역과 배송 상태를 저장·조회한다.
핵심 요구사항: 고객(회원)은 여러 주문을 가진다. 주문 하나에는 여러 상품이 서로 다른 수량으로 담긴다. 상품은 재고와 가격을 가진다. 주문은 상태(대기/결제완료/배송중/완료/취소)를 갖고, 주문 시점의 상품 가격을 따로 기록해야 한다(나중에 상품 가격이 바뀌어도 과거 주문 금액은 유지).
기술 스택/DB: PostgreSQL, ORM은 Prisma 사용 예정

아래 순서로 진행해줘.
1. 요구사항에서 찾아낸 엔티티(개체)와 그들 사이의 관계(1:N, N:M)를 목록으로 정리하고, 내가 놓쳤을 법한 관계를 되물어봐
2. 최소 3정규형(3NF)을 기준으로 테이블 스키마를 설계해 — 각 테이블의 컬럼·타입·제약조건(PK, FK, UNIQUE, NOT NULL, DEFAULT)을 표로 정리해
   - N:M 관계는 조인 테이블로 분리하고, 금액은 정수(최소 화폐 단위)나 DECIMAL로, 시각은 타임존 포함 타입으로
   - 소프트 삭제 여부와 공통 컬럼(created_at, updated_at) 정책도 정해줘
3. 자주 쓰일 조회를 예상해 필요한 인덱스를 제안하고, 각 인덱스가 어떤 쿼리를 위한 것인지 한 줄로
4. 위 설계를 그대로 반영하는 마이그레이션(DDL 또는 ORM 마이그레이션 파일)을 작성해
5. 의도적으로 비정규화한 부분이 있으면 그 이유를 표시해

먼저 1~3번(설계)까지만 주고, 내 승인 뒤에 4번 마이그레이션을 작성해.`,
    exampleNote: "'주문 시점 가격을 따로 기록'이라는 요구를 명시했기 때문에 상품 가격을 주문 항목 테이블에 스냅샷으로 복제하는 의도적 비정규화가 근거와 함께 설계에 반영됩니다.",
    variations: [
      {
        label: "Prisma schema.prisma 버전",
        prompt: `너는 Prisma에 익숙한 시니어 백엔드 개발자야. 설계를 schema.prisma로 표현해줘.

만들 것: [서비스 설명]
핵심 요구사항: [엔티티와 관계]
DB: [예: PostgreSQL]

1. 먼저 엔티티와 관계(1:N, N:M)를 말로 정리하고 애매한 점을 되물어봐
2. 그다음 schema.prisma의 model 정의를 작성해 — 관계 필드, @id, @unique, @default, @relation, 인덱스(@@index)를 정확히 붙이고, N:M은 명시적 조인 모델로 풀어
3. 화폐는 Decimal, 시각은 DateTime, 상태값은 enum으로 표현해
4. 이 스키마로 마이그레이션을 만드는 명령(prisma migrate dev 등)과 주의점을 알려줘

schema.prisma까지 작성한 뒤, 이 설계에서 나중에 바꾸기 가장 어려운 결정이 무엇인지 한 줄로 경고해줘.`,
      },
      {
        label: "기존 DB 진단·개선 버전",
        prompt: `이미 운영 중인 DB의 스키마를 개선하고 싶어. 먼저 진단부터 해줘.

현재 테이블 구조(DDL 또는 설명 붙여넣기):
[현재 스키마]
겪는 문제: [예: 중복 데이터, NULL 남발, 조회가 느림]
DB: [예: MySQL]

1. 정규화 관점에서 문제점(중복, 갱신·삽입·삭제 이상현상, 누락된 제약조건·인덱스)을 심각도 순으로 진단해
2. 개선된 목표 스키마를 제안하되, 대규모 재작성이 아니라 단계적으로 옮길 수 있게 설계해
3. 각 단계를 무중단으로 적용하는 순서(새 컬럼 추가 → 백필 → 코드 전환 → 옛 컬럼 제거)를 짜줘
4. 각 단계의 롤백 방법과 데이터 손실 위험을 표시해

먼저 진단과 목표 스키마까지만 주고, 마이그레이션은 내 승인 후 작성해.`,
      },
    ],
  },
  {
    id: "vibe-be-02",
    title: "탄탄한 API 서버 뼈대 부트스트랩",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["프로젝트구조", "부트스트랩", "에러핸들링", "헬스체크", "설정관리"],
    prompt: `새 백엔드 API 서버의 뼈대를 세우려고 해. 도메인 기능 말고, 튼튼한 기반부터 잡자. 먼저 계획을 세우고 코드는 승인 후에 써줘.

만들 것: [어떤 API 서버인지 한두 문장]
기술 스택: [예: Node.js + Express + TypeScript, 모르면 "추천해줘"]
운영 환경: [예: 도커 컨테이너로 배포 / 서버리스 / 로컬만]

아래 기반 요소를 갖춘 최소 실행 가능한 서버를 만들어줘.
1. 폴더 구조: 라우트/컨트롤러/서비스/설정 계층을 분리한 트리와 각 폴더의 역할 한 줄
2. 앱 부트스트랩: 서버 진입점과 미들웨어 등록 순서(보안 헤더 → 요청 로깅 → 본문 파서 → 라우트 → 에러 핸들러)
3. 설정 로딩: 환경변수를 한곳에서 읽어 검증하는 config 모듈(필수값이 없으면 부팅을 즉시 실패시킴)
4. 헬스체크: GET /health(프로세스 생존)와 GET /ready(DB 등 의존성 연결 확인)를 구분
5. 중앙 에러 핸들링: 커스텀 에러 클래스 + 마지막 에러 미들웨어에서 상태코드와 일관된 JSON 형태로 응답(내부 스택은 로그로만 남기고 응답엔 노출 금지)
6. 요청 로깅과 graceful shutdown(SIGTERM 시 진행 중 요청을 마치고 종료)

먼저 폴더 구조와 각 파일의 역할(1~2번)만 제안하고, 승인하면 파일을 채워줘. 실제 도메인 기능은 넣지 마.`,
    principle: "탄탄한 서버 뼈대 (구조·설정·에러·헬스체크)",
    why: "기능을 붙이기 전에 설정 검증·중앙 에러 처리·헬스체크 같은 공통 기반을 세워두면, 이후 모든 엔드포인트가 일관된 규칙 위에서 만들어지고 운영 중 장애 대응도 쉬워집니다.",
    tip: "헬스체크는 '프로세스가 살아있는지(liveness)'와 '요청을 받을 준비가 됐는지(readiness)'를 반드시 나눠 만드세요 — 로드밸런서와 오케스트레이터가 다르게 씁니다.",
    example: `새 백엔드 API 서버의 뼈대를 세우려고 해. 도메인 기능 말고, 튼튼한 기반부터 잡자. 먼저 계획을 세우고 코드는 승인 후에 써줘.

만들 것: 사내 팀들이 회의실을 예약하는 서비스의 REST API 서버.
기술 스택: Node.js + Express + TypeScript, DB는 PostgreSQL
운영 환경: 도커 컨테이너로 만들어 쿠버네티스에 배포할 예정

아래 기반 요소를 갖춘 최소 실행 가능한 서버를 만들어줘.
1. 폴더 구조: 라우트/컨트롤러/서비스/설정 계층을 분리한 트리와 각 폴더의 역할 한 줄
2. 앱 부트스트랩: 서버 진입점과 미들웨어 등록 순서(보안 헤더 → 요청 로깅 → 본문 파서 → 라우트 → 에러 핸들러)
3. 설정 로딩: 환경변수를 한곳에서 읽어 검증하는 config 모듈(필수값이 없으면 부팅을 즉시 실패시킴)
4. 헬스체크: GET /health(프로세스 생존)와 GET /ready(DB 등 의존성 연결 확인)를 구분
5. 중앙 에러 핸들링: 커스텀 에러 클래스 + 마지막 에러 미들웨어에서 상태코드와 일관된 JSON 형태로 응답(내부 스택은 로그로만 남기고 응답엔 노출 금지)
6. 요청 로깅과 graceful shutdown(SIGTERM 시 진행 중 요청을 마치고 종료)

먼저 폴더 구조와 각 파일의 역할(1~2번)만 제안하고, 승인하면 파일을 채워줘. 실제 도메인 기능은 넣지 마.`,
    exampleNote: "쿠버네티스 배포를 명시하면 /health와 /ready 구분, graceful shutdown, 환경변수 검증이 형식적 장식이 아니라 실제로 필요한 이유가 분명해져 AI가 이를 제대로 구현합니다.",
    variations: [
      {
        label: "NestJS 버전",
        prompt: `NestJS로 API 서버 뼈대를 세우자. 프레임워크의 관례를 최대한 따라줘.

만들 것: [API 서버 설명]
DB/ORM: [예: PostgreSQL + TypeORM]

1. nest 기본 구조 위에 모듈/컨트롤러/서비스 계층을 어떻게 나눌지 제안해
2. ConfigModule로 환경변수를 로딩하고 Joi 등으로 스키마 검증(필수값 누락 시 부팅 실패)
3. 전역 예외 필터로 일관된 에러 응답 형태를 만들고, 전역 ValidationPipe로 DTO 검증을 켜줘
4. Terminus로 /health(liveness)와 /ready(DB 연결 확인) 헬스체크를 구성
5. 요청 로깅 인터셉터와 graceful shutdown 훅

먼저 모듈 구조만 제안하고, 승인하면 채워줘. 도메인 기능은 넣지 마.`,
      },
      {
        label: "FastAPI(Python) 버전",
        prompt: `FastAPI(Python)로 API 서버 뼈대를 세우자.

만들 것: [API 서버 설명]
DB: [예: PostgreSQL + SQLAlchemy]

1. app/ 아래 라우터/서비스/설정/스키마 계층으로 나눈 구조를 제안해
2. pydantic BaseSettings로 환경변수를 읽고 검증(필수값 없으면 시작 시 에러)
3. 커스텀 예외 + 전역 exception handler로 일관된 JSON 에러 응답(내부 상세는 로그로만)
4. GET /health와 GET /ready(DB 세션으로 SELECT 1 확인)를 분리
5. 요청 로깅 미들웨어와 lifespan 이벤트로 시작/종료(연결 정리) 처리

먼저 구조와 각 파일 역할만 주고, 승인하면 코드를 채워줘. 도메인 기능은 넣지 마.`,
      },
    ],
  },
  {
    id: "vibe-be-03",
    title: "리소스 하나의 CRUD REST 엔드포인트 + 입력 검증",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["CRUD", "REST", "입력검증", "SQL인젝션방지"],
    prompt: `리소스 하나에 대한 CRUD REST API를 만들자. 코드 전에 계약(엔드포인트 표)부터 합의하고 진행해줘.

리소스: [리소스 이름 — 예: 할 일(todo), 상품(product)]
필드: [필드 이름·타입·필수 여부 — 예: title(문자열, 필수), done(불리언, 기본 false)]
기술 스택: [예: Node.js + Express + PostgreSQL, 모르면 "추천해줘"]

아래 순서로 진행해줘.
1. REST 관례에 맞는 엔드포인트 표를 먼저 제시하고 멈춰
   - POST /리소스(생성), GET /리소스(목록), GET /리소스/:id(단건), PATCH /리소스/:id(부분 수정), DELETE /리소스/:id(삭제)
   - 각 행에 요청 본문/응답 형태와 성공 상태코드(201/200/204)를 표시
2. 승인하면 컨트롤러 → 서비스 → 데이터 접근 계층으로 나눠 구현해
3. 모든 입력은 경계에서 스키마로 검증해(필수/타입/길이/범위). 검증 실패는 400(또는 422)으로 무엇이 왜 틀렸는지 알려주는 일관된 에러 형태로
4. DB 접근은 반드시 파라미터 바인딩(플레이스홀더)으로 — 문자열을 이어붙인 쿼리는 절대 쓰지 마(SQL 인젝션 방지)
5. 엣지 케이스를 처리해: 없는 id 조회·수정·삭제는 404, 잘못된 JSON은 400, 목록이 커질 수 있으면 페이지네이션은 TODO로 표시
6. 각 엔드포인트를 확인할 curl 예시를 마지막에 붙여줘

먼저 1번 엔드포인트 표만 주고, 내 승인 후 구현으로 넘어가.`,
    principle: "리소스 중심 CRUD (계약 먼저 + 경계 검증)",
    why: "CRUD는 반복적이지만 입력 검증·상태코드·SQL 인젝션 방어를 놓치기 쉽습니다. 엔드포인트 계약을 먼저 확정하고 경계에서 검증하면 안전하고 일관된 API가 나옵니다.",
    tip: "PUT(전체 교체)과 PATCH(부분 수정)를 구분하고, 삭제 성공은 보통 204(본문 없음)로 응답하도록 명시하면 REST 관례가 흐트러지지 않습니다.",
    example: `리소스 하나에 대한 CRUD REST API를 만들자. 코드 전에 계약(엔드포인트 표)부터 합의하고 진행해줘.

리소스: 상품(product)
필드: name(문자열, 필수, 1~100자), price(정수 원 단위, 필수, 0 이상), stock(정수, 기본 0), category(문자열, 선택), is_active(불리언, 기본 true)
기술 스택: Node.js + Express + PostgreSQL(node-postgres로 SQL 직접 작성)

아래 순서로 진행해줘.
1. REST 관례에 맞는 엔드포인트 표를 먼저 제시하고 멈춰
   - POST /products(생성), GET /products(목록), GET /products/:id(단건), PATCH /products/:id(부분 수정), DELETE /products/:id(삭제)
   - 각 행에 요청 본문/응답 형태와 성공 상태코드(201/200/204)를 표시
2. 승인하면 컨트롤러 → 서비스 → 데이터 접근 계층으로 나눠 구현해
3. 모든 입력은 경계에서 스키마로 검증해(필수/타입/길이/범위). 검증 실패는 400(또는 422)으로 무엇이 왜 틀렸는지 알려주는 일관된 에러 형태로
4. DB 접근은 반드시 파라미터 바인딩(플레이스홀더)으로 — 문자열을 이어붙인 쿼리는 절대 쓰지 마(SQL 인젝션 방지)
5. 엣지 케이스를 처리해: 없는 id 조회·수정·삭제는 404, 잘못된 JSON은 400, 목록이 커질 수 있으면 페이지네이션은 TODO로 표시
6. 각 엔드포인트를 확인할 curl 예시를 마지막에 붙여줘

먼저 1번 엔드포인트 표만 주고, 내 승인 후 구현으로 넘어가.`,
    exampleNote: "필드마다 타입과 범위(1~100자, 0 이상)를 못 박아서 AI가 검증 스키마를 구체적으로 생성하고, 'price에 음수' 같은 값까지 400으로 막는 코드를 만듭니다.",
    variations: [
      {
        label: "Prisma ORM 버전",
        prompt: `리소스 하나에 대한 CRUD REST API를 Prisma ORM으로 만들자.

리소스: [리소스 이름]
필드: [필드·타입·필수]
스택: [예: Express + Prisma + PostgreSQL, 또는 Next.js Route Handler]

1. 엔드포인트 표(메서드·경로·요청/응답·상태코드)를 먼저 합의
2. 입력 검증은 zod 스키마로 정의하고, 생성용/수정용 스키마를 분리해(수정은 모든 필드 선택적)
3. Prisma Client로 CRUD를 구현하되, 없는 레코드 에러(P2025)를 잡아 404로 변환해
4. select로 필요한 필드만 반환하고(내부 컬럼 노출 방지), 목록은 기본 limit을 걸어둬
5. 마지막에 각 엔드포인트 curl 예시

엔드포인트 표부터 주고 멈춰.`,
      },
      {
        label: "소유권 검증 포함(인증 사용자) 버전",
        prompt: `로그인한 사용자가 '자기 것만' 다룰 수 있는 CRUD API를 만들자. 인증은 이미 붙어 있다고 가정(요청에 인증된 userId가 담겨 있음).

리소스: [사용자에게 종속된 리소스 — 예: 개인 메모]
필드: [필드·타입]
스택: [예: Express + PostgreSQL]

1. 엔드포인트 표를 먼저 합의(모든 경로는 인증 필요)
2. 생성 시 소유자(user_id)를 요청 본문이 아니라 인증 토큰의 userId로 설정해 — 클라이언트가 소유자를 위조하지 못하게
3. 조회·수정·삭제는 'id가 존재하고 그 소유자가 현재 사용자일 때'만 허용해. 남의 리소스면 404(존재 자체를 숨김)로 응답
4. 입력은 경계에서 검증하고, 쿼리는 파라미터 바인딩만 사용
5. 소유권 검증을 빠뜨리기 쉬운 지점을 주석으로 표시해줘

엔드포인트 표부터 주고 멈춰.`,
      },
    ],
  },
  {
    id: "vibe-be-04",
    title: "환경변수·시크릿 안전하게 관리하기",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["환경변수", "시크릿관리", "설정검증", "12팩터"],
    prompt: `프로젝트의 환경변수와 시크릿을 안전하게 관리하는 체계를 잡자. 코드 전에 어떤 값이 필요한지부터 정리해줘.

프로젝트: [무엇을 하는 서비스인지]
기술 스택: [예: Node.js + TypeScript, 모르면 "추천해줘"]
환경: [예: 로컬 개발 / 스테이징 / 프로덕션]

아래를 만들어줘.
1. 필요한 환경변수 목록을 표로: 이름 · 용도 · 필수/선택 · 예시값 · 비밀 여부(공개 설정 vs 절대 커밋 금지 시크릿)
2. 시작 시점에 환경변수를 한곳에서 읽고 스키마로 검증하는 config 모듈 — 필수값 누락·형식 오류면 서버를 즉시 실패시키고(fail fast) 무엇이 빠졌는지 명확히 출력해. 앱의 나머지 코드는 process.env를 직접 읽지 말고 이 config만 통해 접근
3. .env.example(플레이스홀더만 채운 커밋용 템플릿)과 실제 .env(로컬용, 커밋 금지)의 역할 구분, .gitignore에 무엇을 넣을지
4. 개발/운영 분리 전략: 환경별로 값이 어떻게 갈라지는지, 프로덕션 시크릿은 .env 파일이 아니라 배포 플랫폼의 시크릿 저장소/환경변수 주입으로 관리한다는 원칙
5. 시크릿이 실수로 커밋됐을 때의 대응(키 폐기·회전)과 예방(커밋 전 스캔) 팁

원칙: 비밀값은 절대 코드·로그·저장소에 평문으로 남기지 않는다. 먼저 1번 변수 표부터 만들고 확인받아.`,
    principle: "설정·시크릿 분리 (검증 + fail fast + 최소 노출)",
    why: "환경변수를 코드 여기저기서 직접 읽고 검증 없이 쓰면 프로덕션에서 값 하나 빠져 뒤늦게 터지거나 시크릿이 저장소에 새어 나갑니다. 한곳에서 검증하고 비밀값을 코드 밖으로 빼내면 이런 사고를 예방합니다.",
    tip: ".env는 절대 커밋하지 말고 .env.example만 커밋하세요 — 새 팀원은 example을 복사해 값을 채우면 바로 시작할 수 있습니다.",
    example: `프로젝트의 환경변수와 시크릿을 안전하게 관리하는 체계를 잡자. 코드 전에 어떤 값이 필요한지부터 정리해줘.

프로젝트: 주문 관리 REST API 서버. DB 접속, JWT 인증, 결제사(PG) 연동, 슬랙 알림이 필요하다.
기술 스택: Node.js + Express + TypeScript, 검증은 zod 사용 선호
환경: 로컬 개발과 프로덕션(도커 배포) 분리

아래를 만들어줘.
1. 필요한 환경변수 목록을 표로: 이름 · 용도 · 필수/선택 · 예시값 · 비밀 여부(공개 설정 vs 절대 커밋 금지 시크릿)
2. 시작 시점에 환경변수를 한곳에서 읽고 스키마로 검증하는 config 모듈 — 필수값 누락·형식 오류면 서버를 즉시 실패시키고(fail fast) 무엇이 빠졌는지 명확히 출력해. 앱의 나머지 코드는 process.env를 직접 읽지 말고 이 config만 통해 접근
3. .env.example(플레이스홀더만 채운 커밋용 템플릿)과 실제 .env(로컬용, 커밋 금지)의 역할 구분, .gitignore에 무엇을 넣을지
4. 개발/운영 분리 전략: 환경별로 값이 어떻게 갈라지는지, 프로덕션 시크릿은 .env 파일이 아니라 배포 플랫폼의 시크릿 저장소/환경변수 주입으로 관리한다는 원칙
5. 시크릿이 실수로 커밋됐을 때의 대응(키 폐기·회전)과 예방(커밋 전 스캔) 팁

원칙: 비밀값은 절대 코드·로그·저장소에 평문으로 남기지 않는다. 먼저 1번 변수 표부터 만들고 확인받아.`,
    exampleNote: "DB·JWT·PG·슬랙처럼 비밀값이 섞인 연동을 나열하면 AI가 'DATABASE_URL은 시크릿, PORT는 공개' 식으로 민감도를 구분하고 프로덕션은 시크릿 저장소로 주입하라는 설계를 내놓습니다.",
    variations: [
      {
        label: "서버리스(Vercel) 버전",
        prompt: `서버리스(Vercel) 환경에 맞는 환경변수·시크릿 관리를 잡자. .env 파일이 배포에 올라가지 않는 구조를 전제로.

프로젝트: [서비스 설명]
스택: [예: Next.js(App Router) + TypeScript]

1. 필요한 환경변수 표(이름·용도·필수·비밀 여부)를 만들고, 브라우저에 노출돼도 되는 값(NEXT_PUBLIC_ 접두사)과 서버 전용 시크릿을 반드시 구분해
2. 서버에서 검증하는 env 모듈을 만들되, 클라이언트 코드에서 서버 시크릿을 import하면 빌드가 깨지도록 서버/클라이언트 경계를 지켜줘
3. 로컬은 .env.local(커밋 금지), 배포는 Vercel 프로젝트 환경변수(Preview/Production 분리)로 주입하는 흐름을 설명해
4. NEXT_PUBLIC_에 시크릿을 넣으면 왜 위험한지 한 줄 경고를 남겨줘

변수 표부터 만들고 확인받아.`,
      },
      {
        label: "Python(pydantic Settings) 버전",
        prompt: `Python 프로젝트의 설정·시크릿 관리를 pydantic Settings로 잡자.

프로젝트: [서비스 설명]
스택: [예: FastAPI + Python]

1. 필요한 환경변수 표(이름·용도·필수/선택·비밀 여부·기본값)를 먼저 만들어
2. pydantic-settings의 BaseSettings로 Settings 클래스를 정의해 타입·필수값을 검증하고, 앱 시작 시 로딩 실패하면 즉시 죽게 해. 나머지 코드는 이 Settings 객체(캐시로 싱글턴화)만 통해 값에 접근
3. .env(로컬, 커밋 금지)와 .env.example(커밋용) 구분, .gitignore 항목
4. 프로덕션은 .env가 아니라 컨테이너/플랫폼 환경변수로 주입하는 원칙과, SecretStr로 로그에 시크릿이 찍히지 않게 하는 팁

변수 표부터 주고 멈춰.`,
      },
    ],
  },
  {
    id: "vibe-be-05",
    title: "안전한 DB 마이그레이션과 시드 데이터",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["마이그레이션", "시드데이터", "스키마변경", "롤백", "멱등성"],
    prompt: `DB 스키마 변경을 안전하게 관리하는 마이그레이션 체계와 초기 시드 데이터를 만들자.

현재 상황: [처음 마이그레이션 도입 / 이미 운영 중인 DB에 변경 필요 — 있으면 현재 스키마 요약]
하려는 변경: [예: 특정 테이블에 컬럼 추가, 새 테이블 생성, 컬럼 타입 변경]
기술 스택/도구: [예: PostgreSQL + Prisma Migrate / Knex / Flyway. 모르면 "추천해줘"]

아래를 지켜서 만들어줘.
1. 마이그레이션 파일 명명·버전 관리 규칙과, up(적용)/down(롤백)을 어떻게 쓸지 원칙을 먼저 정해
2. 이번 변경의 마이그레이션을 작성하되, 운영 데이터가 있다고 가정하고 무중단 원칙을 지켜:
   - 파괴적 변경(컬럼 삭제·이름 변경·타입 축소)은 한 번에 하지 말고 '추가 → 백필 → 코드 전환 → 제거'의 확장/수축(expand/contract) 단계로 쪼개
   - NOT NULL 컬럼을 추가할 땐 기본값이나 백필 전략을 함께 제시(기존 행이 깨지지 않게)
   - 큰 테이블 인덱스 생성은 잠금을 피하는 방식(예: PostgreSQL의 CREATE INDEX CONCURRENTLY)을 고려
3. 각 마이그레이션에 대응하는 롤백(down)을 작성하고, 되돌릴 수 없거나 위험한 경우 그 이유와 대안을 표시
4. 시드 데이터: 개발/테스트에 필요한 초기 데이터(참조용 코드값, 관리자 계정 등)를 넣는 스크립트를 만들되, 여러 번 실행해도 중복이 안 생기게 멱등(idempotent)하게(upsert 사용). 비밀번호 같은 값은 평문이 아니라 해시로 저장
5. 로컬에서 마이그레이션 적용·롤백·시드를 실행하는 명령을 정리해

먼저 1번 규칙과 이번 변경의 단계 설계까지만 주고, 승인하면 실제 마이그레이션·시드 파일을 작성해.`,
    principle: "안전한 스키마 진화 (expand/contract + 멱등 시드)",
    why: "운영 중인 DB에서 스키마를 한 번에 바꾸면 배포 순간 기존 데이터나 구버전 코드가 깨질 수 있습니다. 변경을 단계로 쪼개고 롤백과 멱등 시드를 준비하면 무중단으로 안전하게 진화시킬 수 있습니다.",
    tip: "컬럼 삭제·이름 변경은 절대 한 번의 배포로 하지 말고, 새 컬럼을 먼저 추가해 데이터를 옮긴 뒤 다음 배포에서 옛 컬럼을 지우세요.",
    example: `DB 스키마 변경을 안전하게 관리하는 마이그레이션 체계와 초기 시드 데이터를 만들자.

현재 상황: 이미 운영 중인 주문 관리 서비스. orders 테이블에는 결제 여부만 불리언(is_paid)으로 있는데, 이제 '대기/결제완료/배송중/완료/취소' 5단계 상태(status)로 세분화하려 한다. 기존 주문 수만 건이 이미 쌓여 있다.
하려는 변경: orders에 status 컬럼(enum)을 추가하고, 기존 is_paid 값을 기준으로 status를 채운 뒤, 최종적으로 is_paid를 제거하고 싶다.
기술 스택/도구: PostgreSQL + Prisma Migrate, 시드는 Prisma seed 스크립트

아래를 지켜서 만들어줘.
1. 마이그레이션 파일 명명·버전 관리 규칙과, up(적용)/down(롤백)을 어떻게 쓸지 원칙을 먼저 정해
2. 이번 변경의 마이그레이션을 작성하되, 운영 데이터가 있다고 가정하고 무중단 원칙을 지켜:
   - 파괴적 변경(컬럼 삭제·이름 변경·타입 축소)은 한 번에 하지 말고 '추가 → 백필 → 코드 전환 → 제거'의 확장/수축(expand/contract) 단계로 쪼개
   - NOT NULL 컬럼을 추가할 땐 기본값이나 백필 전략을 함께 제시(기존 행이 깨지지 않게)
   - 큰 테이블 인덱스 생성은 잠금을 피하는 방식(예: PostgreSQL의 CREATE INDEX CONCURRENTLY)을 고려
3. 각 마이그레이션에 대응하는 롤백(down)을 작성하고, 되돌릴 수 없거나 위험한 경우 그 이유와 대안을 표시
4. 시드 데이터: 개발/테스트에 필요한 초기 데이터(참조용 코드값, 관리자 계정 등)를 넣는 스크립트를 만들되, 여러 번 실행해도 중복이 안 생기게 멱등(idempotent)하게(upsert 사용). 비밀번호 같은 값은 평문이 아니라 해시로 저장
5. 로컬에서 마이그레이션 적용·롤백·시드를 실행하는 명령을 정리해

먼저 1번 규칙과 이번 변경의 단계 설계까지만 주고, 승인하면 실제 마이그레이션·시드 파일을 작성해.`,
    exampleNote: "'기존 주문 수만 건 + is_paid를 status로 대체'라는 현실적 조건 덕분에 AI가 한 방에 바꾸지 않고 status 추가 → is_paid 기준 백필 → 코드 전환 → is_paid 제거의 다단계 마이그레이션을 설계합니다.",
    variations: [
      {
        label: "순수 SQL 마이그레이션(도구 없이) 버전",
        prompt: `ORM 마이그레이션 도구 없이, 번호를 매긴 순수 SQL 파일로 마이그레이션을 관리하자.

현재 스키마: [테이블 요약 또는 DDL]
하려는 변경: [변경 내용]
DB: [예: PostgreSQL / MySQL]

1. NNN_이름.up.sql / NNN_이름.down.sql 형태의 파일 규칙과, 어떤 마이그레이션이 적용됐는지 추적하는 schema_migrations 테이블을 제안해
2. 이번 변경의 up.sql을 트랜잭션으로 감싸 작성하고(지원하는 DB에서), 파괴적 변경은 단계로 쪼개
3. 대응하는 down.sql을 작성하되 되돌릴 수 없는 부분은 주석으로 경고
4. 이 SQL들을 순서대로 적용하는 방법을 알려줘

파일 규칙과 단계 설계부터 주고 멈춰.`,
      },
      {
        label: "테스트용 대량 더미 데이터 버전",
        prompt: `개발·부하 테스트용으로 현실적인 대량 더미 데이터를 생성하는 시드 스크립트를 만들자.

대상 테이블/관계: [예: users 1만 명, 각 user당 주문 0~20건]
스택/도구: [예: Node.js + Prisma + faker]

1. 관계 무결성을 지키며(부모 먼저, 자식 나중) 데이터를 생성하는 순서를 설계해
2. faker 같은 라이브러리로 그럴듯한 값을 만들되, 재현 가능하도록 시드값을 고정할 수 있게 해
3. 수만 건을 넣을 때 한 건씩 insert하지 말고 배치(createMany 등)로 처리해 성능을 확보
4. 실행 전 기존 데이터를 비우는 옵션과, 실수로 프로덕션에서 돌지 않도록 환경(NODE_ENV) 가드를 넣어줘

먼저 생성 순서와 규모 설계만 주고, 승인하면 스크립트를 작성해.`,
      },
    ],
  },
  {
    id: "vibe-be-06",
    title: "페이지네이션·정렬·필터를 갖춘 목록 API",
    category: "vibe-coding",
    difficulty: "고급",
    tags: ["페이지네이션", "정렬", "필터", "목록API", "커서"],
    prompt: `리소스 목록을 조회하는 엔드포인트를 페이지네이션·정렬·필터를 갖춰 제대로 만들자. 코드 전에 쿼리 파라미터 계약부터 합의해줘.

리소스: [목록으로 보여줄 리소스 — 예: 상품, 게시글]
필터 조건: [무엇으로 걸러야 하는지 — 예: 카테고리, 상태, 검색어, 가격 범위]
정렬 기준: [정렬 가능해야 할 필드 — 예: 생성일, 가격]
기술 스택: [예: Node.js + Express + PostgreSQL, 모르면 "추천해줘"]

아래를 지켜서 설계·구현해줘.
1. 쿼리 파라미터 계약을 표로 먼저 제시하고 멈춰: page/limit(또는 cursor), sort(필드+방향), 각 필터 파라미터, 검색어. 기본값과 허용 최대 limit(예: 100)을 명시
2. 페이지네이션 방식 선택: 오프셋 기반과 커서 기반의 장단점을 이 리소스 기준으로 비교하고 하나를 추천해(대용량·실시간이면 커서 권장). 응답에는 데이터 배열과 함께 페이지 메타(다음 커서 또는 총 개수/현재 페이지)를 담아
3. 정렬·필터는 화이트리스트로만 허용해 — 클라이언트가 넘긴 필드명을 그대로 SQL에 넣지 말고 허용된 컬럼에만 매핑(정렬 필드 주입 방지). 값은 파라미터 바인딩
4. 입력 검증: limit 범위, 알 수 없는 sort 필드·정렬 방향, 잘못된 필터 값은 400으로 반환
5. 성능: 정렬·필터에 쓰이는 컬럼의 인덱스를 제안하고, N+1 쿼리를 피하는 방법을 언급
6. 확인용 curl 예시 몇 개(필터+정렬+페이지 조합)

먼저 1번 파라미터 계약과 2번 방식 추천까지만 주고, 승인하면 구현해.`,
    principle: "견고한 목록 조회 (계약·화이트리스트·커서 페이지네이션)",
    why: "목록 API는 필터·정렬 필드를 그대로 쿼리에 넣다가 주입이 생기거나, 오프셋 페이지네이션으로 대용량에서 느려지기 쉽습니다. 파라미터를 계약으로 고정하고 화이트리스트·인덱스를 갖추면 안전하고 빠릅니다.",
    tip: "정렬·필터 필드는 반드시 허용 목록에 매핑하세요 — 사용자 입력 필드명을 SQL에 직접 넣으면 값 바인딩만으로는 막지 못하는 주입 위험이 생깁니다.",
    example: `리소스 목록을 조회하는 엔드포인트를 페이지네이션·정렬·필터를 갖춰 제대로 만들자. 코드 전에 쿼리 파라미터 계약부터 합의해줘.

리소스: 상품(product)
필터 조건: category(카테고리), is_active(판매중 여부), q(이름 검색어), 가격 범위(minPrice~maxPrice)
정렬 기준: 생성일(created_at), 가격(price), 이름(name) — 오름/내림 모두
기술 스택: Node.js + Express + PostgreSQL(node-postgres로 SQL 직접 작성)

아래를 지켜서 설계·구현해줘.
1. 쿼리 파라미터 계약을 표로 먼저 제시하고 멈춰: page/limit(또는 cursor), sort(필드+방향), 각 필터 파라미터, 검색어. 기본값과 허용 최대 limit(예: 100)을 명시
2. 페이지네이션 방식 선택: 오프셋 기반과 커서 기반의 장단점을 이 리소스 기준으로 비교하고 하나를 추천해(대용량·실시간이면 커서 권장). 응답에는 데이터 배열과 함께 페이지 메타(다음 커서 또는 총 개수/현재 페이지)를 담아
3. 정렬·필터는 화이트리스트로만 허용해 — 클라이언트가 넘긴 필드명을 그대로 SQL에 넣지 말고 허용된 컬럼에만 매핑(정렬 필드 주입 방지). 값은 파라미터 바인딩
4. 입력 검증: limit 범위, 알 수 없는 sort 필드·정렬 방향, 잘못된 필터 값은 400으로 반환
5. 성능: 정렬·필터에 쓰이는 컬럼의 인덱스를 제안하고, N+1 쿼리를 피하는 방법을 언급
6. 확인용 curl 예시 몇 개(필터+정렬+페이지 조합)

먼저 1번 파라미터 계약과 2번 방식 추천까지만 주고, 승인하면 구현해.`,
    exampleNote: "필터·정렬 후보를 구체적으로 나열하면 AI가 'sort=price,desc'를 허용 컬럼에 매핑하는 화이트리스트와 (category, created_at) 복합 인덱스 제안까지 함께 내놓습니다.",
    variations: [
      {
        label: "커서 기반 무한 스크롤 버전",
        prompt: `무한 스크롤 피드를 위한 커서 기반 목록 API를 만들자. 오프셋 방식은 쓰지 마.

리소스: [피드 항목 — 예: 게시글]
정렬: [예: 최신순(created_at 내림차순, 동점은 id로 tie-break)]
스택: [예: Express + PostgreSQL]

1. 요청/응답 계약을 정해: 요청은 limit과 cursor(다음 페이지 토큰), 응답은 items와 nextCursor(더 없으면 null)
2. 커서는 정렬 키(created_at, id)를 조합해 인코딩하고, keyset 조건(정렬 키가 커서 값보다 작은 행)으로 다음 페이지를 가져와 — 정렬 키에 동점이 있어도 항목이 빠지거나 중복되지 않게 tie-break 컬럼을 포함
3. 정렬 키 컬럼에 복합 인덱스를 제안해
4. 커서 위변조·잘못된 커서 값 처리(400)를 넣어줘

계약과 커서 인코딩 방식부터 설명하고 멈춰.`,
      },
      {
        label: "Prisma ORM 버전",
        prompt: `페이지네이션·정렬·필터를 갖춘 목록 API를 Prisma로 만들자.

리소스: [리소스]
필터/정렬: [조건들]
스택: [예: Express + Prisma + PostgreSQL]

1. 쿼리 파라미터를 zod로 검증하는 스키마부터 정의해(limit 상한, sort 필드는 enum으로 제한 = 화이트리스트)
2. 필터를 Prisma where 객체로 동적 조립하되, 들어온 필터만 조건에 추가해(빈 값은 무시)
3. 페이지네이션은 Prisma의 cursor + take로 커서 방식으로 구현하고, 응답에 nextCursor를 포함
4. select로 필요한 필드만 반환하고, 연관 데이터는 include로 한 번에 가져와 N+1을 피해
5. 정렬·필터 컬럼 인덱스를 schema.prisma의 @@index로 제안

검증 스키마와 파라미터 계약부터 주고 멈춰.`,
      },
    ],
  },
  {
    id: "vibe-be-07",
    title: "OpenAPI/Swagger로 API 문서 자동 생성",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["OpenAPI", "Swagger", "API문서", "자동생성"],
    prompt: `우리 REST API의 문서를 OpenAPI(Swagger)로 만들고, 코드와 어긋나지 않게 유지하는 방법까지 잡자.

API 개요: [무엇을 하는 API인지, 주요 리소스]
기술 스택: [예: Node.js + Express + TypeScript, 모르면 "추천해줘"]
현재 상태: [엔드포인트가 이미 있음(코드 붙여넣기 가능) / 이제 문서부터 설계]

아래 순서로 진행해줘.
1. 문서화 접근을 먼저 추천해: (a) 코드에서 자동 생성(주석/데코레이터 또는 zod·타입에서 스키마 추출)과 (b) OpenAPI 명세를 먼저 쓰고 그에 맞춰 구현(design-first)의 장단점을 우리 상황 기준으로 비교
2. OpenAPI 3 문서의 뼈대를 만들어: info(제목·버전), servers, 공통 components(재사용 스키마, 에러 응답 형태, 보안 스킴)
3. 각 엔드포인트마다: 경로·메서드·요약, 파라미터/요청 본문 스키마, 성공·실패 응답(상태코드별)과 예시를 정의. 요청·응답 스키마는 재사용 컴포넌트로 참조(ref)해 중복을 없애
4. 인증이 있으면 보안 스킴(예: Bearer JWT)을 정의하고 보호된 엔드포인트에 표시. 문서에 실제 시크릿·토큰·내부 URL을 넣지 마
5. Swagger UI를 붙여 브라우저에서 열어보고 요청을 시험할 수 있게 하되, 프로덕션에서 문서 공개 범위를 어떻게 제한할지 언급
6. 문서가 실제 코드와 어긋나는 것을 막는 방법(스키마를 단일 소스로 삼아 검증에도 재사용, 또는 CI에서 명세-구현 일치 검사)을 제안

먼저 1번 접근 추천과 2번 뼈대까지만 주고, 승인하면 엔드포인트별로 채워줘.`,
    principle: "코드와 일치하는 API 문서 (OpenAPI 자동화)",
    why: "손으로 쓴 API 문서는 금세 코드와 어긋나 신뢰를 잃습니다. 스키마를 단일 소스로 삼아 OpenAPI를 생성하면 문서가 항상 실제 동작과 맞고, Swagger UI로 즉시 시험해볼 수 있습니다.",
    tip: "요청 검증에 쓰는 스키마(zod 등)에서 OpenAPI를 함께 뽑아내면 '검증'과 '문서'가 한 소스가 되어 서로 어긋날 일이 없습니다.",
    example: `우리 REST API의 문서를 OpenAPI(Swagger)로 만들고, 코드와 어긋나지 않게 유지하는 방법까지 잡자.

API 개요: 주문 관리 REST API. 상품 조회, 주문 생성·조회, 주문 상태 변경 엔드포인트가 있고 인증은 JWT를 쓴다.
기술 스택: Node.js + Express + TypeScript, 입력 검증은 이미 zod로 하고 있음
현재 상태: 엔드포인트가 이미 구현돼 있고 zod 스키마도 있음 — 이걸 문서의 단일 소스로 재사용하고 싶어

아래 순서로 진행해줘.
1. 문서화 접근을 먼저 추천해: (a) 코드에서 자동 생성(주석/데코레이터 또는 zod·타입에서 스키마 추출)과 (b) OpenAPI 명세를 먼저 쓰고 그에 맞춰 구현(design-first)의 장단점을 우리 상황 기준으로 비교
2. OpenAPI 3 문서의 뼈대를 만들어: info(제목·버전), servers, 공통 components(재사용 스키마, 에러 응답 형태, 보안 스킴)
3. 각 엔드포인트마다: 경로·메서드·요약, 파라미터/요청 본문 스키마, 성공·실패 응답(상태코드별)과 예시를 정의. 요청·응답 스키마는 재사용 컴포넌트로 참조(ref)해 중복을 없애
4. 인증이 있으면 보안 스킴(예: Bearer JWT)을 정의하고 보호된 엔드포인트에 표시. 문서에 실제 시크릿·토큰·내부 URL을 넣지 마
5. Swagger UI를 붙여 브라우저에서 열어보고 요청을 시험할 수 있게 하되, 프로덕션에서 문서 공개 범위를 어떻게 제한할지 언급
6. 문서가 실제 코드와 어긋나는 것을 막는 방법(스키마를 단일 소스로 삼아 검증에도 재사용, 또는 CI에서 명세-구현 일치 검사)을 제안

먼저 1번 접근 추천과 2번 뼈대까지만 주고, 승인하면 엔드포인트별로 채워줘.`,
    exampleNote: "'이미 zod 스키마가 있다'고 알려주면 AI가 별도 문서를 새로 쓰는 대신 검증 스키마에서 OpenAPI를 생성하는, 문서-코드 불일치가 원천 차단되는 방식을 추천합니다.",
    variations: [
      {
        label: "FastAPI 자동 문서 버전",
        prompt: `FastAPI의 자동 문서 기능을 최대한 살려 API 문서를 정돈하자.

API 개요: [무엇을 하는 API인지]
스택: FastAPI + Python + pydantic

1. FastAPI가 pydantic 모델과 타입 힌트에서 OpenAPI를 자동 생성한다는 점을 활용해, 각 경로 함수에 response_model·상태코드·summary·description·tags를 제대로 붙이는 방법을 보여줘
2. 요청/응답 pydantic 모델에 예시와 필드 설명을 달아 문서 품질을 높여
3. 에러 응답(4xx/5xx)을 responses로 명시하고, 공통 에러 모델을 재사용해
4. 인증(OAuth2/Bearer) 보안 스킴을 정의해 보호된 엔드포인트에 표시하고, /docs(Swagger UI)와 /redoc의 공개 범위를 프로덕션에서 어떻게 제한할지 알려줘

먼저 어떤 모델·엔드포인트에 무엇을 보강할지 목록으로 주고, 승인하면 코드에 반영해.`,
      },
      {
        label: "design-first(명세 먼저) 버전",
        prompt: `구현 전에 OpenAPI 명세를 먼저 확정하는 design-first 방식으로 진행하자.

만들 API: [무엇을 하는 API인지, 주요 리소스와 동작]
인증: [예: JWT Bearer]

1. openapi.yaml(OpenAPI 3.1) 뼈대를 작성해: info, servers, components(공통 스키마·에러·보안 스킴)
2. 합의한 엔드포인트마다 경로·파라미터·요청/응답 스키마·상태코드·예시를 명세에 정의하고, 스키마는 components로 재사용(ref)해 중복을 없애
3. 이 명세로부터 (a) 서버 라우트 스텁, (b) 클라이언트 타입/SDK를 생성하는 방법을 알려줘 — 명세가 계약(단일 진실 공급원)이 되도록
4. 명세를 검증(lint)하는 방법과, 구현이 명세를 어기지 않는지 CI에서 확인하는 흐름을 제안해

먼저 openapi.yaml 뼈대와 엔드포인트 목록부터 주고, 승인하면 각 엔드포인트를 채워.`,
      },
    ],
  },
];
