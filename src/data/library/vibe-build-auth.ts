import type { LibraryPrompt } from '../types';

export const prompts: LibraryPrompt[] = [
  {
    id: "vibe-auth-01",
    title: "이메일·비밀번호 회원가입/로그인 (안전한 해싱)",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["인증", "비밀번호해싱", "bcrypt", "회원가입", "보안"],
    prompt: `너는 인증 보안에 밝은 시니어 백엔드 개발자야. 아직 코드는 쓰지 말고 먼저 계획부터 세워줘.

만들 기능: 이메일/비밀번호 기반 회원가입과 로그인
기술 스택: [기술 스택 — 예: Next.js Route Handler + Prisma + PostgreSQL]
제약: [있으면 — 예: 외부 인증 서비스 없이 직접 구현]

먼저 아래를 계획으로 정리해줘. 코드는 내 승인 후에 하나씩.
1. 회원가입·로그인 흐름을 각각 단계로 그려줘 (요청 → 검증 → 저장/조회 → 응답)
2. 만들 파일/엔드포인트 목록과 각각의 역할
3. 아래 보안 원칙을 어디서 어떻게 지킬지 명시해줘

반드시 지킬 보안 원칙:
- 비밀번호는 절대 평문으로 저장·로그·응답하지 않는다. bcrypt(cost 12 이상) 또는 argon2id로 해시해서만 저장한다.
- 이메일/비밀번호 형식을 서버에서 검증한다(이메일 형식, 최소 길이, 흔한 비밀번호 거부). 클라이언트 검증만 믿지 않는다.
- 로그인 실패 메시지는 이메일과 비밀번호 중 어느 쪽이 틀렸는지 구분하지 않는다(계정 존재 여부 노출 방지).
- 존재하지 않는 계정도 더미 해시 비교를 수행해 응답 시간 차이로 계정 유무가 드러나지 않게 한다.
- 이메일 중복 가입은 막되, 회원가입 응답으로 기존 계정 존재를 노출하지 않는다.
- 로그인 시도에 rate limit을 걸어 무차별 대입을 막는다.

마지막에, 이 스택에서 개발자가 흔히 놓치는 보안 함정 2~3개를 경고로 정리해줘.

계획을 내가 승인하면 1번 흐름부터 하나씩 구현하자.`,
    principle: "비밀번호는 해시로만 저장 (평문 금지)",
    why: "인증의 첫 관문은 자격증명을 안전하게 다루는 것입니다. 계획을 먼저 세우고 해싱·검증·타이밍 공격 방어를 명시하면, 되돌리기 어려운 보안 결함을 코드로 굳히기 전에 막을 수 있습니다.",
    tip: "'이 스택의 흔한 함정을 경고해줘'를 꼭 넣으세요. 프레임워크마다 실수 지점이 달라 맞춤 경고를 얻을 수 있습니다.",
    example: `너는 인증 보안에 밝은 시니어 백엔드 개발자야. 아직 코드는 쓰지 말고 먼저 계획부터 세워줘.

만들 기능: 이메일/비밀번호 기반 회원가입과 로그인
기술 스택: Next.js(App Router) Route Handler + Prisma + PostgreSQL, 해싱은 argon2id
제약: 외부 인증 서비스(Auth0 등) 없이 직접 구현, 사이드 프로젝트지만 실제 배포 예정

먼저 아래를 계획으로 정리해줘. 코드는 내 승인 후에 하나씩.
1. 회원가입·로그인 흐름을 각각 단계로 그려줘 (요청 → 검증 → 저장/조회 → 응답)
2. 만들 파일/엔드포인트 목록과 각각의 역할
3. 아래 보안 원칙을 어디서 어떻게 지킬지 명시해줘

반드시 지킬 보안 원칙:
- 비밀번호는 절대 평문으로 저장·로그·응답하지 않는다. bcrypt(cost 12 이상) 또는 argon2id로 해시해서만 저장한다.
- 이메일/비밀번호 형식을 서버에서 검증한다(이메일 형식, 최소 길이, 흔한 비밀번호 거부). 클라이언트 검증만 믿지 않는다.
- 로그인 실패 메시지는 이메일과 비밀번호 중 어느 쪽이 틀렸는지 구분하지 않는다(계정 존재 여부 노출 방지).
- 존재하지 않는 계정도 더미 해시 비교를 수행해 응답 시간 차이로 계정 유무가 드러나지 않게 한다.
- 이메일 중복 가입은 막되, 회원가입 응답으로 기존 계정 존재를 노출하지 않는다.
- 로그인 시도에 rate limit을 걸어 무차별 대입을 막는다.

마지막에, 이 스택에서 개발자가 흔히 놓치는 보안 함정 2~3개를 경고로 정리해줘.

계획을 내가 승인하면 1번 흐름부터 하나씩 구현하자.`,
    exampleNote: "스택과 해싱 알고리즘(argon2id)까지 못 박아서 AI가 Prisma 스키마의 비밀번호 컬럼 처리, Route Handler에서의 서버 검증 위치, 타이밍 공격 방어 지점을 구체적으로 짚어줍니다.",
    variations: [
      {
        label: "매직링크(비밀번호 없는) 버전",
        prompt: `너는 인증 보안에 밝은 시니어 개발자야. 코드 전에 흐름부터 설계하자.

만들 기능: 비밀번호 없이 이메일로 받은 일회용 매직링크로 로그인
기술 스택: [기술 스택 — 예: Next.js + 이메일 발송 서비스]

먼저 계획.
1. 매직링크 흐름을 단계로 그려줘 (이메일 입력 → 토큰 생성·저장 → 메일 발송 → 링크 클릭 → 검증 → 세션 발급)
2. 만들 엔드포인트와 저장할 데이터

반드시 지킬 보안 원칙:
- 링크 토큰은 예측 불가능한 난수로 만들고, 서버에는 해시해서 저장한다(평문 토큰 저장 금지).
- 토큰은 짧은 만료(예: 10분)와 일회용(사용 즉시 폐기)으로 한다.
- 같은 이메일로의 발송 요청에 rate limit을 걸고, 계정 존재 여부를 응답으로 노출하지 않는다(존재하든 아니든 동일 안내).
- 토큰 검증은 상수 시간 비교로 하고, 링크는 HTTPS로만 연다.

마지막에 매직링크 방식의 흔한 함정(메일 스캐너의 사전 클릭, 링크 재사용 등)과 대응을 정리해줘. 계획 승인 후 토큰 생성부터 구현하자.`,
      },
      {
        label: "Supabase Auth 위임 버전",
        prompt: `너는 Supabase에 익숙한 시니어 개발자야. 코드 전에 설정부터 점검하자.

만들 기능: Supabase Auth로 이메일/비밀번호 회원가입·로그인 (해싱은 Supabase가 처리)
기술 스택: Next.js(App Router) + Supabase

먼저 계획.
1. 회원가입·로그인·세션 유지 흐름을 Supabase 클라이언트/서버 컴포넌트 관점에서 그려줘
2. 서버에서 세션을 신뢰성 있게 읽는 방법(쿠키 처리)과 만들 파일 목록

반드시 지킬 보안 원칙:
- Supabase가 비밀번호를 해싱하더라도, 서버 라우트 보호는 세션을 서버에서 검증해서 한다(클라이언트 판단만 믿지 않음).
- Row Level Security(RLS)를 켜고 테이블별 접근 정책을 명시한다. RLS 없이 anon 키로 데이터가 열리지 않게 한다.
- service_role 키는 서버 전용으로만 쓰고 브라우저에 노출하지 않는다.
- 이메일 인증(confirm) 여부에 따른 접근 제한과 비밀번호 정책을 설정한다.

마지막에 Supabase 인증에서 개발자가 흔히 놓치는 함정(RLS 미설정, service_role 노출)을 경고해줘. 계획 승인 후 설정부터 잡자.`,
      },
    ],
  },
  {
    id: "vibe-auth-02",
    title: "세션 vs JWT 인증 (access/refresh 토큰 흐름)",
    category: "vibe-coding",
    difficulty: "고급",
    tags: ["인증", "JWT", "세션", "리프레시토큰", "쿠키보안"],
    prompt: `너는 인증 아키텍처에 밝은 시니어 개발자야. 코드 전에 방식 선택과 설계부터 하자.

만들 것: 로그인 후 인증 상태 유지 — 세션 방식과 JWT(access/refresh) 방식 중 내 상황에 맞는 것을 골라 구현
기술 스택: [기술 스택 — 예: Express API + React SPA]
클라이언트 종류: [웹 SPA / 서버 렌더링 / 모바일 앱 등]

먼저 결정과 설계부터.
1. 내 상황(클라이언트 종류, 로그아웃 즉시성·확장 요구)에서 세션 vs JWT 중 무엇이 나은지 근거와 함께 추천해줘
2. 고른 방식의 수명주기를 그려줘 — 발급 → 저장 위치 → 만료 → 갱신 → 로그아웃(무효화)
3. 만들 엔드포인트와 미들웨어 목록

반드시 지킬 보안 원칙:
- 토큰/세션 식별자는 httpOnly, Secure, SameSite 쿠키에 담는다. refresh 토큰을 localStorage에 두지 않는다(XSS 노출).
- access 토큰 수명은 짧게(예: 15분), refresh 토큰은 길게 두되 회전(rotation)시키고, 재사용이 감지되면 해당 토큰 계열 전체를 폐기한다.
- refresh 토큰은 서버에 저장/해시해 두어 로그아웃·탈취 시 무효화할 수 있게 한다(순수 무상태 JWT의 로그아웃 불가 문제를 인지).
- JWT는 서버에서 alg를 고정하고(none 알고리즘 금지), 서명·만료·issuer/audience를 검증한다.
- 서명 비밀키는 환경변수로 관리하고 코드/저장소에 하드코딩하지 않는다.

마지막에 세션과 JWT의 트레이드오프(로그아웃 즉시성, 확장성, 복잡도)를 표로 정리하고, 흔한 함정을 경고해줘.

계획 승인 후 갱신(refresh) 흐름부터 구현하자.`,
    principle: "짧은 access + 회전하는 refresh, 토큰은 httpOnly 쿠키",
    why: "세션과 JWT는 각각 트레이드오프가 있어 상황에 맞게 골라야 합니다. 수명·저장 위치·무효화를 먼저 설계하면 XSS 토큰 탈취나 로그아웃 불가 같은 구조적 결함을 예방할 수 있습니다.",
    tip: "'로그아웃이 즉시 반영돼야 하나?'를 스스로 답해 보세요. 이 요구 하나가 세션이냐 JWT냐를 크게 가릅니다.",
    example: `너는 인증 아키텍처에 밝은 시니어 개발자야. 코드 전에 방식 선택과 설계부터 하자.

만들 것: 로그인 후 인증 상태 유지 — 세션 방식과 JWT(access/refresh) 방식 중 내 상황에 맞는 것을 골라 구현
기술 스택: Express API + React SPA(별도 도메인), DB는 PostgreSQL
클라이언트 종류: 브라우저 웹 SPA. 관리자 강제 로그아웃 기능이 필요하고, 곧 서버를 여러 대로 늘릴 예정

먼저 결정과 설계부터.
1. 내 상황(클라이언트 종류, 로그아웃 즉시성·확장 요구)에서 세션 vs JWT 중 무엇이 나은지 근거와 함께 추천해줘
2. 고른 방식의 수명주기를 그려줘 — 발급 → 저장 위치 → 만료 → 갱신 → 로그아웃(무효화)
3. 만들 엔드포인트와 미들웨어 목록

반드시 지킬 보안 원칙:
- 토큰/세션 식별자는 httpOnly, Secure, SameSite 쿠키에 담는다. refresh 토큰을 localStorage에 두지 않는다(XSS 노출).
- access 토큰 수명은 짧게(예: 15분), refresh 토큰은 길게 두되 회전(rotation)시키고, 재사용이 감지되면 해당 토큰 계열 전체를 폐기한다.
- refresh 토큰은 서버에 저장/해시해 두어 로그아웃·탈취 시 무효화할 수 있게 한다(순수 무상태 JWT의 로그아웃 불가 문제를 인지).
- JWT는 서버에서 alg를 고정하고(none 알고리즘 금지), 서명·만료·issuer/audience를 검증한다.
- 서명 비밀키는 환경변수로 관리하고 코드/저장소에 하드코딩하지 않는다.

마지막에 세션과 JWT의 트레이드오프(로그아웃 즉시성, 확장성, 복잡도)를 표로 정리하고, 흔한 함정을 경고해줘.

계획 승인 후 갱신(refresh) 흐름부터 구현하자.`,
    exampleNote: "'관리자 강제 로그아웃'과 '서버 다대 확장'이라는 상충하는 요구를 함께 주면, AI가 두 요구를 모두 만족시키는 절충안(서버 저장 refresh 토큰 회전)을 근거와 함께 추천합니다.",
    variations: [
      {
        label: "세션 기반(Redis 저장) 버전",
        prompt: `너는 세션 인증에 밝은 시니어 개발자야. 코드 전에 설계부터 하자.

만들 것: 서버 세션 기반 로그인 — 세션 저장소로 [저장소 — 예: Redis] 사용
기술 스택: [기술 스택 — 예: Express + Redis]

먼저 계획.
1. 세션 수명주기를 그려줘 (로그인 → 세션 생성·저장 → 쿠키로 세션ID 전달 → 요청마다 조회 → 만료/로그아웃 시 삭제)
2. 만들 미들웨어와 엔드포인트

반드시 지킬 보안 원칙:
- 세션ID는 예측 불가능한 난수, 쿠키는 httpOnly·Secure·SameSite로 설정한다.
- 로그인 성공 시 세션 고정(session fixation) 방지를 위해 세션ID를 재발급한다.
- 세션에 만료(TTL)를 두고, 로그아웃 시 서버 저장소에서 즉시 삭제한다.
- 상태 변경 요청에는 CSRF 방어(토큰 또는 SameSite)를 적용한다.

마지막에 세션 방식의 흔한 함정(세션 고정, CSRF, 저장소 장애 대응)을 정리해줘. 계획 승인 후 세션 생성부터 구현하자.`,
      },
      {
        label: "모바일 앱용 토큰 버전",
        prompt: `너는 모바일 백엔드에 밝은 시니어 개발자야. 코드 전에 설계부터 하자.

만들 것: 네이티브 모바일 앱을 위한 토큰 인증 (쿠키를 쓰기 어려운 환경)
기술 스택: [기술 스택 — 예: Node API + iOS/Android 앱]

먼저 계획.
1. access/refresh 토큰 발급·갱신 흐름과, 앱에서 토큰을 어디에 보관할지 그려줘
2. 만들 엔드포인트와 토큰 검증 미들웨어

반드시 지킬 보안 원칙:
- refresh 토큰은 앱의 보안 저장소(iOS Keychain / Android Keystore)에 보관하도록 안내한다. 일반 저장소에 평문 보관 금지.
- access 토큰은 짧게, refresh 토큰은 회전(rotation)시키고 재사용 감지 시 폐기한다.
- 서버는 토큰을 저장/해시해 원격 로그아웃(기기별 무효화)을 지원한다.
- 모든 통신은 TLS로만, JWT는 alg 고정·서명·만료를 검증한다.

마지막에 모바일 토큰 관리의 흔한 함정(토큰 평문 저장, 무한 수명 refresh)을 경고해줘. 계획 승인 후 발급 흐름부터 구현하자.`,
      },
    ],
  },
  {
    id: "vibe-auth-03",
    title: "소셜 로그인(OAuth) — Google/Kakao 콜백·계정 연동",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["OAuth", "소셜로그인", "계정연동", "PKCE", "리다이렉트검증"],
    prompt: `너는 OAuth 연동 경험이 많은 시니어 개발자야. 구현 전에 흐름부터 정리하자.

만들 것: 소셜 로그인 — [제공자 목록 — 예: Google, Kakao]
기술 스택: [기술 스택 — 예: Next.js(App Router)]
기존 인증: [이미 이메일 로그인이 있는지 / 소셜만 쓸지]

먼저 계획.
1. Authorization Code 흐름을 단계로 그려줘 (인증 요청 → 콜백 → 토큰 교환 → 프로필 조회 → 세션 발급)
2. 제공자별 콜백 URL·필요한 스코프(최소한만)·설정할 환경변수 목록
3. 같은 이메일의 기존 계정이 있을 때 계정 연동(account linking)을 어떻게 처리할지

반드시 지킬 보안 원칙:
- redirect_uri(콜백 URL)를 허용 목록으로 고정 검증한다. 열린 리다이렉트를 만들지 않는다.
- CSRF 방지를 위해 state 파라미터를 발급·검증하고, 지원하면 PKCE를 사용한다.
- 클라이언트 시크릿은 서버에서만 쓰고 브라우저/저장소에 노출하지 않는다.
- 소셜에서 받은 이메일이 검증된(verified) 이메일인지 확인하고, 미검증 이메일로 기존 계정을 자동 연동하지 않는다(계정 탈취 방지).
- 필요한 최소 스코프만 요청한다.

마지막에 제공자별로 다른 점(스코프 이름, 이메일 제공 여부 등)과 흔한 함정을 정리해줘.

계획 승인 후 한 제공자부터 구현하자.`,
    principle: "state/PKCE로 CSRF 방지, redirect_uri 고정, 검증된 이메일만 연동",
    why: "OAuth는 단계가 많아 콜백 검증·계정 연동에서 사고가 잦습니다. 흐름과 연동 규칙을 먼저 그리면 열린 리다이렉트나 미검증 이메일 자동 병합 같은 계정 탈취 경로를 막을 수 있습니다.",
    tip: "제공자를 하나씩 붙이세요. Google로 흐름을 완성한 뒤 Kakao를 추가하면 제공자별 차이(이메일 미제공 등)를 안전하게 흡수할 수 있습니다.",
    example: `너는 OAuth 연동 경험이 많은 시니어 개발자야. 구현 전에 흐름부터 정리하자.

만들 것: 소셜 로그인 — Google, Kakao
기술 스택: Next.js(App Router) + Prisma + PostgreSQL, 세션은 httpOnly 쿠키
기존 인증: 이미 이메일/비밀번호 로그인이 있고, 소셜 로그인을 추가로 붙이려 함

먼저 계획.
1. Authorization Code 흐름을 단계로 그려줘 (인증 요청 → 콜백 → 토큰 교환 → 프로필 조회 → 세션 발급)
2. 제공자별 콜백 URL·필요한 스코프(최소한만)·설정할 환경변수 목록
3. 같은 이메일의 기존 계정이 있을 때 계정 연동(account linking)을 어떻게 처리할지

반드시 지킬 보안 원칙:
- redirect_uri(콜백 URL)를 허용 목록으로 고정 검증한다. 열린 리다이렉트를 만들지 않는다.
- CSRF 방지를 위해 state 파라미터를 발급·검증하고, 지원하면 PKCE를 사용한다.
- 클라이언트 시크릿은 서버에서만 쓰고 브라우저/저장소에 노출하지 않는다.
- 소셜에서 받은 이메일이 검증된(verified) 이메일인지 확인하고, 미검증 이메일로 기존 계정을 자동 연동하지 않는다(계정 탈취 방지).
- 필요한 최소 스코프만 요청한다.

마지막에 제공자별로 다른 점(스코프 이름, 이메일 제공 여부 등)과 흔한 함정을 정리해줘.

계획 승인 후 한 제공자부터 구현하자.`,
    exampleNote: "'이미 이메일 로그인이 있다'는 맥락 덕분에 AI가 소셜 계정을 기존 계정에 안전하게 연결하는 규칙(이메일 검증 확인 후 연동, 미검증이면 별도 확인 절차)을 핵심으로 설계합니다.",
    variations: [
      {
        label: "NextAuth(Auth.js) 사용 버전",
        prompt: `너는 Auth.js에 익숙한 시니어 개발자야. 코드 전에 구성부터 잡자.

만들 것: NextAuth(Auth.js)로 [제공자 — 예: Google, Kakao] 소셜 로그인
기술 스택: Next.js(App Router) + Auth.js + [DB 어댑터 — 예: Prisma]

먼저 계획.
1. Auth.js provider·adapter·callback 구성을 어떻게 잡을지와 만들 파일 목록
2. 세션 전략(JWT vs database)과 서버에서 세션을 검증하는 방법

반드시 지킬 보안 원칙:
- AUTH_SECRET과 각 provider의 클라이언트 시크릿을 환경변수로만 관리한다.
- 콜백 URL을 각 provider 콘솔에 정확히 등록하고, 운영/개발 도메인을 분리한다.
- signIn 콜백에서 이메일 검증 여부를 확인해 계정 연동 정책을 적용한다(미검증 자동 병합 금지).
- 세션·라우트 보호는 서버에서 검증한다.

마지막에 Auth.js 사용 시 흔한 함정(콜백 URL 불일치, 자동 계정 병합 위험)을 경고해줘. 계획 승인 후 한 provider부터 붙이자.`,
      },
      {
        label: "기존 계정에 소셜 연결 버전",
        prompt: `너는 OAuth 계정 연동에 밝은 시니어 개발자야. 코드 전에 흐름부터 정하자.

만들 것: 이미 로그인한 사용자가 자기 계정에 소셜 계정을 '추가로 연결'하는 기능 (로그인 대체가 아니라 연결)
기술 스택: [기술 스택 — 예: Next.js + Prisma]

먼저 계획.
1. 로그인된 상태에서 소셜 연결 흐름을 그려줘 (연결 시작 → 콜백 → 현재 로그인 사용자와 매핑)
2. 한 사용자에 여러 소셜 계정을 매핑하는 데이터 구조와, 연결 해제(unlink) 처리

반드시 지킬 보안 원칙:
- 연결 요청은 반드시 현재 로그인 세션 소유자에게만 귀속시킨다(state에 사용자 맥락을 안전하게 묶고 서버에서 검증).
- 이미 다른 계정에 연결된 소셜 계정을 가로채 연결하지 못하게 중복을 검사한다.
- 마지막 로그인 수단을 해제하면 계정 잠김이 되므로, unlink 시 최소 한 개의 로그인 수단이 남는지 검증한다.
- state·CSRF 방어를 그대로 적용한다.

마지막에 계정 연결 기능의 흔한 함정(세션 탈취를 통한 강제 연결, 최후 수단 해제)을 정리해줘. 계획 승인 후 연결 흐름부터 구현하자.`,
      },
    ],
  },
  {
    id: "vibe-auth-04",
    title: "인증 미들웨어 & 권한(RBAC) — 보호 라우트",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["인증미들웨어", "RBAC", "권한", "접근제어", "IDOR방지"],
    prompt: `너는 접근제어 설계에 밝은 시니어 개발자야. 코드 전에 권한 모델부터 정하자.

만들 것: 인증 미들웨어와 역할 기반 접근제어(RBAC) — 보호된 라우트와 역할별 권한
기술 스택: [기술 스택 — 예: Express + JWT]
역할과 권한: [역할 목록과 각자 할 수 있는 것 — 예: admin / editor / user]

먼저 계획.
1. 인증(누구인가) 미들웨어와 인가(무엇을 할 수 있나) 미들웨어를 분리해서 설계해줘
2. 역할 → 권한 매핑 표를 만들고, 라우트별로 필요한 권한을 정리해줘
3. 미들웨어를 라우트에 어떻게 붙일지 예시 구조

반드시 지킬 보안 원칙:
- 기본은 거부(deny by default). 명시적으로 허용된 역할만 통과시킨다.
- 권한 판정은 항상 서버에서 한다. 클라이언트가 보낸 역할 값을 신뢰하지 않고, 검증된 토큰/세션의 역할을 쓴다.
- 리소스 소유권을 확인한다(예: 본인 글만 수정). 역할만이 아니라 '이 사용자가 이 리소스에 권한이 있나'까지 검사한다(IDOR 방지).
- 인증 실패(401)와 인가 실패(403)를 구분하되, 권한 없는 리소스의 존재 여부는 노출하지 않는다.
- 권한 체크를 개별 핸들러에 흩뿌리지 말고 미들웨어/정책 계층으로 모은다.

마지막에 RBAC로 부족해지는 시점(속성 기반 ABAC이 필요한 경우)과 흔한 함정을 알려줘.

계획 승인 후 인증 미들웨어부터 구현하자.`,
    principle: "기본 거부 + 서버에서 권한 판정 + 소유권까지 확인(IDOR 방지)",
    why: "권한 버그는 조용히 데이터 유출로 이어집니다. 인증과 인가를 분리하고 역할·소유권 검사를 정책 계층으로 모으면, 라우트마다 빠지는 검사 없이 일관되게 접근을 통제할 수 있습니다.",
    tip: "역할 검사만으로는 부족합니다. 'user 역할이 남의 리소스 ID를 넣으면?'을 꼭 물어 IDOR(소유권 미검사) 취약점을 짚으세요.",
    example: `너는 접근제어 설계에 밝은 시니어 개발자야. 코드 전에 권한 모델부터 정하자.

만들 것: 인증 미들웨어와 역할 기반 접근제어(RBAC) — 보호된 라우트와 역할별 권한
기술 스택: Express + JWT(access 토큰), DB는 PostgreSQL
역할과 권한: admin(모든 글 관리·사용자 관리), editor(자기 글 CRUD + 남의 글 읽기), user(자기 글 읽기·작성만)

먼저 계획.
1. 인증(누구인가) 미들웨어와 인가(무엇을 할 수 있나) 미들웨어를 분리해서 설계해줘
2. 역할 → 권한 매핑 표를 만들고, 라우트별로 필요한 권한을 정리해줘
3. 미들웨어를 라우트에 어떻게 붙일지 예시 구조

반드시 지킬 보안 원칙:
- 기본은 거부(deny by default). 명시적으로 허용된 역할만 통과시킨다.
- 권한 판정은 항상 서버에서 한다. 클라이언트가 보낸 역할 값을 신뢰하지 않고, 검증된 토큰/세션의 역할을 쓴다.
- 리소스 소유권을 확인한다(예: 본인 글만 수정). 역할만이 아니라 '이 사용자가 이 리소스에 권한이 있나'까지 검사한다(IDOR 방지).
- 인증 실패(401)와 인가 실패(403)를 구분하되, 권한 없는 리소스의 존재 여부는 노출하지 않는다.
- 권한 체크를 개별 핸들러에 흩뿌리지 말고 미들웨어/정책 계층으로 모은다.

마지막에 RBAC로 부족해지는 시점(속성 기반 ABAC이 필요한 경우)과 흔한 함정을 알려줘.

계획 승인 후 인증 미들웨어부터 구현하자.`,
    exampleNote: "editor가 '자기 글만' 수정 가능하다는 조건을 명시하면, AI가 역할 통과 이후에도 리소스 소유권을 재확인하는 정책 계층을 설계해 IDOR를 원천 차단합니다.",
    variations: [
      {
        label: "Next.js 미들웨어 버전",
        prompt: `너는 Next.js 접근제어에 밝은 시니어 개발자야. 코드 전에 설계부터 하자.

만들 것: Next.js(App Router) 보호 라우트 + 역할 기반 접근제어
기술 스택: Next.js(App Router) + [세션 방식 — 예: 쿠키 세션]

먼저 계획.
1. middleware.ts(엣지)에서의 대략적 게이트와, 서버 컴포넌트/Route Handler에서의 실제 권한 검증을 어떻게 나눌지
2. 역할 → 접근 가능 경로 매핑과 보호 대상 목록

반드시 지킬 보안 원칙:
- middleware의 리다이렉트만으로 보안을 끝내지 않는다. 데이터에 닿는 서버 지점(Route Handler·서버 액션)에서 반드시 다시 검증한다.
- 역할은 클라이언트가 아니라 서버에서 검증한 세션에서 읽는다.
- 리소스 접근은 소유권까지 확인한다(IDOR 방지).
- 기본 거부 원칙으로 보호 경로를 화이트리스트가 아닌 방식으로 빠뜨리지 않게 한다.

마지막에 Next.js에서 흔한 함정(middleware만 믿기, 서버 액션 검증 누락)을 경고해줘. 계획 승인 후 검증 유틸부터 만들자.`,
      },
      {
        label: "세분화된 권한(퍼미션) 버전",
        prompt: `너는 권한 모델 설계에 밝은 시니어 개발자야. 코드 전에 모델부터 정하자.

만들 것: 역할 대신(또는 역할 위에) 세분화된 권한(permission) 기반 접근제어
기술 스택: [기술 스택]
권한 예시: [예: post.create, post.delete.any, user.manage]

먼저 계획.
1. 권한 목록을 정의하고, 역할을 '권한의 묶음'으로 매핑하는 구조를 설계해줘
2. 라우트/액션마다 요구 권한을 선언적으로 붙이는 방법(예: requirePermission)

반드시 지킬 보안 원칙:
- 기본 거부. 요구 권한을 명시하지 않은 보호 리소스는 통과시키지 않는다.
- 권한은 서버의 검증된 신원에서 계산하고, 캐시하더라도 권한 변경 시 반영되게 한다.
- '남의 리소스'에 대한 권한(.any)과 '자기 리소스'에 대한 권한(.own)을 분리하고, own은 소유권을 확인한다.
- 권한 판정 로직을 한 곳으로 모아 테스트 가능하게 만든다.

마지막에 권한 세분화의 흔한 함정(권한 폭발, own/any 혼동)을 정리해줘. 계획 승인 후 권한 정의부터 만들자.`,
      },
    ],
  },
  {
    id: "vibe-auth-05",
    title: "결제 연동 — 토스페이먼츠/Stripe 체크아웃 흐름",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["결제", "토스페이먼츠", "Stripe", "체크아웃", "금액검증"],
    prompt: `너는 결제 연동 경험이 많은 시니어 개발자야. 돈이 오가는 기능이라 신중하게, 코드 전에 흐름부터 정리하자.

만들 것: 결제 체크아웃 연동 — [결제사 — 예: 토스페이먼츠 또는 Stripe]
기술 스택: [기술 스택 — 예: React + Node/Express]
결제 대상: [무엇을 파는지 — 예: 단건 상품 / 구독]

먼저 계획.
1. 결제 흐름을 단계로 그려줘 (주문 생성 → 결제 요청 → 인증/승인 → 서버 승인 확정 → 주문 완료)
2. 프론트/백엔드 각각의 책임과 만들 엔드포인트
3. 결제 성공을 '무엇으로 최종 확정'할지(리다이렉트 응답만 믿지 않고 서버 승인 API로 확정)

반드시 지킬 보안 원칙:
- 결제 금액과 주문 내용은 서버가 보유한 값으로 검증한다. 클라이언트가 보낸 금액을 그대로 믿고 승인하지 않는다(금액 위변조 방지).
- 시크릿(API) 키는 서버에서만 쓰고 프론트에 노출하지 않는다. 프론트는 공개 키만 사용한다.
- 결제 승인은 서버-투-서버 승인 API로 확정하고, 결제창 리다이렉트 결과만으로 주문을 완료 처리하지 않는다.
- 주문 생성 시 서버에서 주문ID와 금액을 만들어 저장하고, 승인 단계에서 그 값과 대사(reconcile)한다.
- 승인·주문 상태 갱신에 멱등 처리를 걸어 중복 승인·중복 주문을 막는다.

마지막에 결제창 리다이렉트만 믿을 때 생기는 사고 시나리오와, 왜 웹훅으로 별도 확정이 필요한지 짚어줘(웹훅 처리는 다음 단계).

계획 승인 후 주문 생성부터 구현하자.`,
    principle: "금액은 서버가 검증·확정, 시크릿 키는 서버 전용, 승인은 서버 API로",
    why: "결제는 실패가 곧 금전 손실입니다. 금액을 서버가 소유·검증하고 승인을 서버-투-서버로 확정하도록 먼저 설계하면, 금액 위변조와 '결제창만 닫고 미결제' 같은 흔한 사고를 막을 수 있습니다.",
    tip: "'클라이언트가 보낸 금액을 믿지 마라'를 반드시 넣으세요. 이 한 줄이 결제 조작 취약점 대부분을 예방합니다.",
    example: `너는 결제 연동 경험이 많은 시니어 개발자야. 돈이 오가는 기능이라 신중하게, 코드 전에 흐름부터 정리하자.

만들 것: 결제 체크아웃 연동 — 토스페이먼츠
기술 스택: React(프론트) + Node/Express(백엔드), DB는 PostgreSQL
결제 대상: 단건 상품 결제(장바구니 합계 1건)

먼저 계획.
1. 결제 흐름을 단계로 그려줘 (주문 생성 → 결제 요청 → 인증/승인 → 서버 승인 확정 → 주문 완료)
2. 프론트/백엔드 각각의 책임과 만들 엔드포인트
3. 결제 성공을 '무엇으로 최종 확정'할지(리다이렉트 응답만 믿지 않고 서버 승인 API로 확정)

반드시 지킬 보안 원칙:
- 결제 금액과 주문 내용은 서버가 보유한 값으로 검증한다. 클라이언트가 보낸 금액을 그대로 믿고 승인하지 않는다(금액 위변조 방지).
- 시크릿(API) 키는 서버에서만 쓰고 프론트에 노출하지 않는다. 프론트는 공개 키만 사용한다.
- 결제 승인은 서버-투-서버 승인 API로 확정하고, 결제창 리다이렉트 결과만으로 주문을 완료 처리하지 않는다.
- 주문 생성 시 서버에서 주문ID와 금액을 만들어 저장하고, 승인 단계에서 그 값과 대사(reconcile)한다.
- 승인·주문 상태 갱신에 멱등 처리를 걸어 중복 승인·중복 주문을 막는다.

마지막에 결제창 리다이렉트만 믿을 때 생기는 사고 시나리오와, 왜 웹훅으로 별도 확정이 필요한지 짚어줘(웹훅 처리는 다음 단계).

계획 승인 후 주문 생성부터 구현하자.`,
    exampleNote: "토스페이먼츠 단건 결제로 구체화하면, AI가 결제창 성공 리다이렉트에서 받은 paymentKey·orderId·amount를 서버 승인 API로 재확정하고 저장된 금액과 대사하는 안전한 확정 단계를 설계합니다.",
    variations: [
      {
        label: "Stripe Checkout(호스티드) 버전",
        prompt: `너는 Stripe 연동에 밝은 시니어 개발자야. 코드 전에 흐름부터 정리하자.

만들 것: Stripe Checkout(호스티드 결제 페이지)로 단건 결제
기술 스택: [기술 스택 — 예: Next.js + Node]

먼저 계획.
1. Checkout Session 흐름을 그려줘 (서버에서 세션 생성 → Stripe 결제 페이지로 이동 → success/cancel 리다이렉트 → 웹훅으로 최종 확정)
2. 만들 엔드포인트와 저장할 주문 데이터

반드시 지킬 보안 원칙:
- 결제 금액·품목은 서버에서 Checkout Session을 만들 때 서버 소유 값으로 설정한다. 클라이언트가 보낸 금액을 신뢰하지 않는다.
- secret key는 서버에서만, publishable key만 프론트에서 사용한다.
- success 리다이렉트만으로 결제 완료 처리하지 않고, 결제 완료는 웹훅(checkout.session.completed)으로 확정한다.
- 주문과 Session을 매핑해 두어 중복·불일치를 대사한다.

마지막에 리다이렉트만 신뢰할 때의 위험과 웹훅 확정의 필요성을 짚어줘. 계획 승인 후 세션 생성부터 구현하자.`,
      },
      {
        label: "구독 결제 버전",
        prompt: `너는 구독 결제(정기결제)에 밝은 시니어 개발자야. 코드 전에 설계부터 하자.

만들 것: 월간 구독 결제 — [결제사 — 예: Stripe Billing 또는 토스페이먼츠 빌링]
기술 스택: [기술 스택]

먼저 계획.
1. 구독 흐름을 그려줘 (플랜 선택 → 카드 등록/빌링키 발급 → 구독 생성 → 정기 청구 → 갱신/실패/해지)
2. 구독 상태(active, past_due, canceled 등)를 어디에 저장하고 무엇으로 갱신할지

반드시 지킬 보안 원칙:
- 빌링키·결제수단 토큰 같은 민감 정보는 결제사에 보관하고, 우리 서버는 참조용 식별자만 저장한다(카드 원본 저장 금지).
- 정기 청구 결과와 상태 변경은 웹훅으로 확정하고, 멱등 처리한다.
- 해지·환불·연체(past_due) 상태 전이를 명확히 정의하고, 접근 권한(구독자 전용 기능)을 서버에서 상태로 검증한다.
- 금액·플랜은 서버가 소유한 값으로 관리한다.

마지막에 구독 결제의 흔한 함정(웹훅 누락으로 상태 불일치, 연체 처리 누락)을 정리해줘. 계획 승인 후 플랜·구독 모델부터 만들자.`,
      },
    ],
  },
  {
    id: "vibe-auth-06",
    title: "결제 웹훅 처리 — 서명 검증·멱등성·상태 갱신",
    category: "vibe-coding",
    difficulty: "고급",
    tags: ["결제웹훅", "서명검증", "멱등성", "주문상태", "webhook"],
    prompt: `너는 결제 웹훅을 여러 번 다뤄본 시니어 백엔드 개발자야. 코드 전에 처리 설계부터 하자.

만들 것: [결제사 — 예: Stripe 또는 토스페이먼츠] 결제 웹훅 처리 — 서명 검증, 멱등성, 주문 상태 갱신
기술 스택: [기술 스택 — 예: Node/Express]
관련 상태: [주문/결제 상태 값 — 예: pending, paid, failed, canceled]

먼저 계획.
1. 웹훅 수신 흐름을 단계로 그려줘 (수신 → 서명 검증 → 이벤트 파싱 → 멱등 확인 → 상태 갱신 → 200 응답)
2. 어떤 이벤트를 구독하고, 각 이벤트가 주문 상태를 어떻게 바꾸는지 표로
3. 실패·재시도 시에도 안전하도록 멱등성을 어디서 보장할지

반드시 지킬 보안 원칙:
- 모든 웹훅은 서명(secret)으로 검증한다. 검증 실패면 즉시 거부하고, 검증 전 본문을 신뢰해 처리하지 않는다.
- 서명 검증은 가공되지 않은 원문(raw body)으로 한다. 파서가 본문을 바꾸기 전에 원문을 확보한다.
- 멱등성: 이벤트 ID를 저장해 같은 이벤트가 여러 번 와도 한 번만 처리한다(결제사는 재시도를 보낸다).
- 상태 전이는 정해진 방향으로만 허용한다(예: 이미 paid인 주문을 순서가 뒤바뀐 웹훅이 pending으로 되돌리지 못하게).
- 빠르게 200을 돌려주고, 무거운 작업은 큐로 넘긴다(타임아웃 시 재시도가 오므로 멱등성이 전제).
- 웹훅으로 온 금액·통화가 서버 주문과 일치하는지 대사한다.

마지막에 '웹훅을 못 받거나 늦게 받는' 경우를 대비한 폴링/조회 보완책과 흔한 함정을 정리해줘.

계획 승인 후 서명 검증부터 구현하자.`,
    principle: "서명 검증(raw body) + 이벤트 ID 멱등성 + 단방향 상태 전이",
    why: "웹훅은 위조·중복·순서 뒤바뀜이 일상입니다. 서명 검증과 멱등 처리, 허용된 상태 전이만 먼저 설계하면 가짜 결제 완료나 중복 배송 같은 금전 사고를 막을 수 있습니다.",
    tip: "'raw body로 서명 검증'을 명시하세요. JSON 파서가 본문을 먼저 건드리면 서명이 깨지는 것이 가장 흔한 실수입니다.",
    example: `너는 결제 웹훅을 여러 번 다뤄본 시니어 백엔드 개발자야. 코드 전에 처리 설계부터 하자.

만들 것: Stripe 결제 웹훅 처리 — 서명 검증, 멱등성, 주문 상태 갱신
기술 스택: Node/Express, DB는 PostgreSQL
관련 상태: pending, paid, failed, canceled, refunded

먼저 계획.
1. 웹훅 수신 흐름을 단계로 그려줘 (수신 → 서명 검증 → 이벤트 파싱 → 멱등 확인 → 상태 갱신 → 200 응답)
2. 어떤 이벤트를 구독하고, 각 이벤트가 주문 상태를 어떻게 바꾸는지 표로
3. 실패·재시도 시에도 안전하도록 멱등성을 어디서 보장할지

반드시 지킬 보안 원칙:
- 모든 웹훅은 서명(secret)으로 검증한다. 검증 실패면 즉시 거부하고, 검증 전 본문을 신뢰해 처리하지 않는다.
- 서명 검증은 가공되지 않은 원문(raw body)으로 한다. 파서가 본문을 바꾸기 전에 원문을 확보한다.
- 멱등성: 이벤트 ID를 저장해 같은 이벤트가 여러 번 와도 한 번만 처리한다(결제사는 재시도를 보낸다).
- 상태 전이는 정해진 방향으로만 허용한다(예: 이미 paid인 주문을 순서가 뒤바뀐 웹훅이 pending으로 되돌리지 못하게).
- 빠르게 200을 돌려주고, 무거운 작업은 큐로 넘긴다(타임아웃 시 재시도가 오므로 멱등성이 전제).
- 웹훅으로 온 금액·통화가 서버 주문과 일치하는지 대사한다.

마지막에 '웹훅을 못 받거나 늦게 받는' 경우를 대비한 폴링/조회 보완책과 흔한 함정을 정리해줘.

계획 승인 후 서명 검증부터 구현하자.`,
    exampleNote: "Stripe와 구체 상태값을 주면, AI가 Express에서 해당 라우트만 raw body로 받아 서명 검증 후 처리하는 구성과, 이벤트 ID를 유니크 키로 저장하는 멱등 테이블 설계를 함께 제시합니다.",
    variations: [
      {
        label: "토스페이먼츠 웹훅 버전",
        prompt: `너는 토스페이먼츠 연동에 밝은 시니어 백엔드 개발자야. 코드 전에 설계부터 하자.

만들 것: 토스페이먼츠 웹훅 처리 — 결제 상태 변경 수신과 주문 갱신
기술 스택: [기술 스택 — 예: Node/Express]

먼저 계획.
1. 웹훅 수신·검증·상태 갱신 흐름을 그려줘
2. 웹훅과 별개로, 승인 API 응답을 '결제 사실의 1차 근거'로 삼고 웹훅을 보조 확정으로 쓰는 이중 확인 구조

반드시 지킬 보안 원칙:
- 웹훅 수신 시 결제사에 결제 정보를 다시 조회(조회 API)해 진위를 확인한다(수신 데이터만으로 확정하지 않음).
- 이벤트/결제 키 기준으로 멱등 처리해 중복 수신을 한 번만 반영한다.
- 상태 전이는 허용된 방향으로만, 금액·주문 대사를 통과한 경우에만 갱신한다.
- 빠르게 200 응답, 무거운 후처리는 분리한다.

마지막에 웹훅 유실·중복·조회 API 활용에 관한 흔한 함정을 정리해줘. 계획 승인 후 검증·조회부터 구현하자.`,
      },
      {
        label: "큐(비동기 처리) 도입 버전",
        prompt: `너는 대량 웹훅을 안정적으로 처리해 본 시니어 백엔드 개발자야. 코드 전에 아키텍처부터 정하자.

만들 것: 웹훅 수신을 큐 기반으로 처리 — 빠른 수신 확정과 안전한 비동기 처리 분리
기술 스택: [기술 스택 — 예: Node + 메시지 큐]

먼저 계획.
1. '수신 엔드포인트는 검증·저장 후 즉시 200' + '워커가 큐에서 꺼내 상태 갱신'으로 나눈 구조를 그려줘
2. 재시도·실패(Dead Letter) 처리와 멱등성을 어디서 보장할지

반드시 지킬 보안 원칙:
- 수신 단계에서 서명 검증(raw body)을 먼저 하고, 통과한 이벤트만 큐에 넣는다.
- 이벤트 ID로 멱등 처리해 큐 재시도가 있어도 한 번만 반영되게 한다.
- 워커 처리 실패 시 재시도 정책과 무한 재시도 방지(DLQ)를 둔다.
- 상태 전이는 단방향·대사 통과 조건을 워커에서도 그대로 지킨다.

마지막에 큐 도입 시의 흔한 함정(순서 보장 착각, 중복 처리, DLQ 방치)을 정리해줘. 계획 승인 후 수신·큐 적재부터 구현하자.`,
      },
    ],
  },
  {
    id: "vibe-auth-07",
    title: "파일 업로드 — 이미지 검증과 S3/스토리지 연동",
    category: "vibe-coding",
    difficulty: "중급",
    tags: ["파일업로드", "이미지", "S3", "presignedURL", "타입검증"],
    prompt: `너는 파일 업로드 보안에 밝은 시니어 개발자야. 코드 전에 흐름과 검증부터 정하자.

만들 것: 이미지 파일 업로드 — 용량/타입 검증과 [스토리지 — 예: AWS S3] 연동
기술 스택: [기술 스택 — 예: Next.js + Node]
용도: [무엇에 쓰는 이미지인지 — 예: 프로필 사진 / 상품 이미지]

먼저 계획.
1. 업로드 방식을 두 가지로 비교해줘 — (a) 서버 경유 업로드 (b) presigned URL로 클라이언트가 스토리지에 직접 업로드 — 그리고 내 상황에 맞는 쪽 추천
2. 만들 엔드포인트와 저장 경로/파일명 규칙
3. 어디서 무엇을 검증할지(클라이언트/서버/스토리지 각 단계)

반드시 지킬 보안 원칙:
- 파일 크기 상한을 서버에서 강제한다(클라이언트 검증만 믿지 않음). presigned 방식이면 정책으로 최대 크기를 건다.
- 파일 타입을 확장자만이 아니라 실제 내용(매직 넘버)으로 검증하고, 허용 타입 화이트리스트만 통과시킨다.
- 파일명을 그대로 쓰지 않는다. 무작위 값으로 새로 만들어 경로 조작·덮어쓰기·파일명 기반 XSS를 막는다.
- 업로드 파일은 실행 불가능한 위치/버킷에 두고, 응답 시 적절한 Content-Type과 Content-Disposition을 설정한다.
- 스토리지 자격증명은 최소 권한(least privilege)으로, presigned URL은 짧은 만료와 특정 경로·타입으로 제한한다. 버킷은 기본 비공개로 둔다.
- 필요하면 이미지를 재인코딩(리사이즈)해 숨은 악성 페이로드를 제거한다.

마지막에 '공개 버킷 실수'와 'SVG 업로드로 인한 XSS' 같은 흔한 사고와 대응을 정리해줘.

계획 승인 후 검증 로직부터 구현하자.`,
    principle: "타입은 내용으로 검증 + 파일명 무작위화 + 최소 권한·비공개 버킷",
    why: "업로드는 외부 입력이 서버에 파일로 남는 위험한 통로입니다. 검증 위치와 저장 정책을 먼저 정하면, 위장 확장자·경로 조작·공개 버킷 유출 같은 사고를 구현 전에 차단할 수 있습니다.",
    tip: "'확장자 말고 매직 넘버로 검증'을 넣으세요. .jpg로 위장한 실행 파일·스크립트를 걸러내는 핵심 방어입니다.",
    example: `너는 파일 업로드 보안에 밝은 시니어 개발자야. 코드 전에 흐름과 검증부터 정하자.

만들 것: 이미지 파일 업로드 — 용량/타입 검증과 AWS S3 연동
기술 스택: Next.js(App Router) + Node, AWS SDK
용도: 사용자 프로필 사진(1인 1장, 교체 가능)

먼저 계획.
1. 업로드 방식을 두 가지로 비교해줘 — (a) 서버 경유 업로드 (b) presigned URL로 클라이언트가 스토리지에 직접 업로드 — 그리고 내 상황에 맞는 쪽 추천
2. 만들 엔드포인트와 저장 경로/파일명 규칙
3. 어디서 무엇을 검증할지(클라이언트/서버/스토리지 각 단계)

반드시 지킬 보안 원칙:
- 파일 크기 상한을 서버에서 강제한다(클라이언트 검증만 믿지 않음). presigned 방식이면 정책으로 최대 크기를 건다.
- 파일 타입을 확장자만이 아니라 실제 내용(매직 넘버)으로 검증하고, 허용 타입 화이트리스트만 통과시킨다.
- 파일명을 그대로 쓰지 않는다. 무작위 값으로 새로 만들어 경로 조작·덮어쓰기·파일명 기반 XSS를 막는다.
- 업로드 파일은 실행 불가능한 위치/버킷에 두고, 응답 시 적절한 Content-Type과 Content-Disposition을 설정한다.
- 스토리지 자격증명은 최소 권한(least privilege)으로, presigned URL은 짧은 만료와 특정 경로·타입으로 제한한다. 버킷은 기본 비공개로 둔다.
- 필요하면 이미지를 재인코딩(리사이즈)해 숨은 악성 페이로드를 제거한다.

마지막에 '공개 버킷 실수'와 'SVG 업로드로 인한 XSS' 같은 흔한 사고와 대응을 정리해줘.

계획 승인 후 검증 로직부터 구현하자.`,
    exampleNote: "프로필 사진(1인 1장)으로 좁히면, AI가 presigned URL 방식과 사용자ID 기반 무작위 경로, 짧은 만료·크기·Content-Type 제한을 건 안전한 직접 업로드 설계를 추천합니다.",
    variations: [
      {
        label: "서버 경유 업로드(Multer) 버전",
        prompt: `너는 서버 업로드 처리에 밝은 시니어 개발자야. 코드 전에 검증부터 설계하자.

만들 것: 서버를 경유하는 이미지 업로드 (예: Multer 등으로 수신 후 검증·저장)
기술 스택: [기술 스택 — 예: Express]

먼저 계획.
1. 업로드 수신 → 검증 → 저장 흐름과 만들 엔드포인트
2. 임시 저장·검증 통과 후 최종 저장으로 옮기는 절차

반드시 지킬 보안 원칙:
- 요청 단계에서 파일 크기·개수 상한을 강제한다(대용량으로 인한 서비스 거부 방지).
- 확장자·MIME이 아니라 실제 내용(매직 넘버)으로 타입을 검증하고 화이트리스트만 통과시킨다.
- 저장 파일명은 무작위로 재생성하고, 사용자 입력 경로를 그대로 쓰지 않는다(경로 조작 방지).
- 업로드 디렉터리는 실행 권한이 없는 위치로 두고, 웹에서 직접 실행되지 않게 한다.
- 가능하면 이미지를 재인코딩해 메타데이터·악성 페이로드를 제거한다.

마지막에 서버 경유 방식의 흔한 함정(무제한 크기, 경로 조작, 실행 가능한 저장 위치)을 정리해줘. 계획 승인 후 검증 미들웨어부터 구현하자.`,
      },
      {
        label: "이미지 리사이즈/썸네일 버전",
        prompt: `너는 이미지 파이프라인을 구축해 본 시니어 개발자야. 코드 전에 처리 흐름부터 정하자.

만들 것: 업로드 이미지의 리사이즈·썸네일 생성과 [스토리지 — 예: S3] 저장
기술 스택: [기술 스택 — 예: Node + 이미지 처리 라이브러리]

먼저 계획.
1. 원본 검증 → 재인코딩/리사이즈 → 썸네일 생성 → 저장 흐름을 그려줘
2. 원본·썸네일의 저장 경로 규칙과, 동기/비동기(큐) 처리 선택

반드시 지킬 보안 원칙:
- 재인코딩 전에 매직 넘버로 타입을 검증하고, 허용 포맷만 처리한다(위장 파일 차단).
- 처리 과정에서 과도한 해상도·프레임을 제한해 자원 고갈(이미지 폭탄)을 막는다.
- 파일명은 무작위, 버킷은 비공개, presigned URL은 짧은 만료로 제공한다.
- 재인코딩으로 EXIF 등 불필요한 메타데이터와 숨은 페이로드를 제거한다.

마지막에 이미지 처리의 흔한 함정(이미지 폭탄, 메타데이터 유출, 원본 무검증)을 정리해줘. 계획 승인 후 검증·리사이즈부터 구현하자.`,
      },
    ],
  },
];
